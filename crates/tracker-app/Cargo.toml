[package]
name = "tracker-app"
version = "0.3.0"
edition = "2021"

[dependencies]
tracker-core = { path = "../tracker-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
eframe = "0.29"
egui = "0.29"
