# Plan — Image Tracker (bar path MVP)

Decisions and vocabulary: see [CONTEXT.md](CONTEXT.md) and [docs/adr/](docs/adr/).

## Rules

- Every task is T-shirt sized **S** or **M**. Anything that grows to L/XL must be split before work starts.
- Work is TDD (red → green → refactor). A task is *done* only with passing tests and a commit.
- One task = one commit (or a few small ones). Update this file's status column in the same commit that finishes the task.
- Status values: `todo` · `in-progress (worker)` · `done (worker, date)`. Observations column for surprises/decisions.

- **Quality gate**: at every milestone completion (and any major change), fable-5 runs `/brooks-audit` and records findings in the review log; serious findings become tasks before the next milestone starts.
- **Versioning**: bump workspace version and create an annotated git tag at each milestone completion (`v0.1.0` = milestone 1, `v0.2.0` = milestone 2, …). Tag only after the audit gate passes.

## Roles

- **sonnet-5** — worker. Implements one task per run, TDD, one commit per task, updates its PLAN.md row.
- **fable-5** — orchestrator + reviewer. Dispatches tasks, then reviews every delivery: correctness, logic errors, edge cases, missed TDD tests, and **visual verification** of anything with visual output (workers cannot see images — CSV plausibility alone has already produced false "good" verdicts twice). Review findings become fix tasks or inline fixes; both are recorded here.

### Review log (fable-5)

| Date | Finding | Action |
|------|---------|--------|
| 2026-07-14 | 1.2 broke workspace build (removed `version()` still called by tracker-app); worker had only tested its own crate | Fixed inline; all later workers must verify workspace-wide |
| 2026-07-15 | 3.4 "good" verdicts false: rotation metadata ignored → scrambled frames; found by viewing overlay frames | Dispatched 3.5 (rotation fix + e2e rerun) |
| 2026-07-15 | Post-3.5 tracking still drifts (v1 path wanders plate face; v4 slid down body); fixed seed template is stale as plate rotates/lighting shifts; stricter min_score → reseed storm (234 events) | Dispatched 3.6 (adaptive dual-template) |
| 2026-07-15 | 3.6 visual review PASSED: v3/v4 tight vertical rep columns with marker on plate; v1 good (reps + re-rack walk captured); v2 reps captured but path wanders during post-set idle (bar racked, lifter leaves) — acceptable, users track one set. Known limitation recorded: tracking past the end of the set produces noise; future task could add end-of-set trim | Milestone 3 accepted; audit gate + tag next |
| 2026-07-15 | Audit gate (milestones 1–3): layering clean (core dep-free, app→core only, no cycles). Findings: (1) real `unwrap()` in session.rs gap close — rule violation; (2) clippy: NaN-unsafe length check in calibration.rs, 2 while-let loops, dead test assertion; (3) `spawn_tracking` has 9 args, wants a params struct; (4) app.rs at 872 lines — split before 7.2 | (1)+(2) fixed inline by fable-5; (3) → task 3.7; (4) → folded into 7.2. Tagged v0.3.0 |
| 2026-07-15 | Audit gate (milestone 8): pass — tracing confined to tracker-app (core still dependency-free), clippy zero, per-frame events at trace level (no flood). One documented invariant `expect` in ffmpeg_sink write path — acceptable. Tag deferred: versions follow numbered milestone order (v0.4.0 lands when milestone 4 closes) | Continue milestone 4 |
| 2026-07-15 | Audit gate (milestone 4 + 8): pass — core still dependency-free, clippy zero, 179 tests green, `Tracker` trait cleanly generalizes session (no app changes needed). app.rs now 927 lines — 7.2 split becoming urgent, do it before more GUI features | Tagged v0.4.0 (includes milestone 8 observability) |
| 2026-07-15 | First real user GUI session (amadeu01): 2 bugs — tracking ran past video end ("frame 1990" of 1909); lost marker wandered to rack after bar left frame. UX gaps: no calibrate guidance, small/unclear nav controls, no visible outcome/results, no export from GUI, no session restart without killing app, app unnamed in Linux launcher, phases/reps invisible | Grilled → milestone 10 (10.1–10.8) + ROADMAP.md created for deferred features |
| 2026-07-15 | Audit gate (milestone 5): pass — core dep-free, clippy 0, 229 tests. CI failures caught+fixed: newer-rustc f32-fallback lint (video_panel), release e2e asserting on `*.reps.csv` instead of per-frame CSV (same glob bug smoke kit hit — pattern: any `find *.csv` must exclude reps files). v0.5.0 re-tagged after fix; release published with linux x86_64 + mac arm64 tarballs | Milestones 1–6, 7.1/7.2, 8, 9.1–9.4 all done. Remaining: 7.3, 9.5 (deferred) |
| 2026-07-16 | Audit gate (milestone 11): pass — core dep-free (Preprocessor is pure pixel math), clippy 0, 363 tests. docs/theory.md landed with 4 web-verified citations; benchmark results recorded in §7 (v1: gaussian template safest; v3: no filter needed — matches ZNCC-invariance theory). One worker died pre-commit (11.3) — fable-5 verified + committed its work; 11.4 worker instructed to verify `git status` clean, complied | Tagged v0.7.0 |
| 2026-07-17 | 13.7 visual review PASSED (fable-5, dark-mode harness screenshot vs mock): near-black #141416 shell, #1d1d21 toolbar with dark bordered buttons, quiet hint strip, 460px #18181b side panel, rounded #1f1f24 cards for every section, loss-colored table borders/values, chart with dashed thresholds, filmstrip collapsed to its toggle once results exist, monospace status bar — the GUI now genuinely resembles the design. Accepted deviations: default fonts (no IBM Plex), guide/status/settings/events cards remain (mock shows results-only column), filmstrip toggle row visible, plain letterbox | 13.7 accepted — user's "not even close" finding closed. Remaining milestone-13 work: 13.6 live panel |
| 2026-07-17 | 13.4 visual review PASSED (fable-5, decaying-sinusoid harness): chart card renders line, loss-colored dots (green/amber/red), dashed labeled −10/−20/−30 thresholds, 1-based x labels, unit-aware y ticks; selected rep 3 = larger dot + table row + scrub segment all reading the same `selected_rep`. `linear_trend` in tracker-core TDD'd. USER FINDING (2026-07-17): GUI overall "not even close" to the design — correct; root cause is 13.1 never wired `ChromePalette` into egui's global `Style`/`Visuals`, so the shell still renders stock egui. Full 7-point gap audit written into docs/design/vbt-tracker-ui-notes.md → new task 13.7 | 13.4 accepted; 13.7 grilled (design-fidelity pass) |
| 2026-07-17 | 14.2 review PASSED (fable-5): settings extracted to settings_section.rs (side_panel 1024→760 lines — the audit-flagged growth addressed), 15 hover tooltips + education consts under test, Docs link via `ui.hyperlink_to` (URL checked live, HTTP 200); chrome-unregression screenshot clean. NOTE: the GitHub §7 anchor resolves only after main is pushed — theory.md's deep-dive exists only locally until then | 14.2 accepted; push main so the Docs link lands on §7 |
| 2026-07-17 | 14.1 review (fable-5 + rust-ddd-reviewer): overlays visually verified — sampled frames show `none-template` path drifting up the plates vs `gaussian1.5-color` holding tight to the seed; the strategy comparison works as intended. Reviewer findings (both low, fixed inline by fable-5): (1) `Timebase::new` failure aborted metric-only runs — now lazy, only constructed under `--export-overlays`; (2) one overlay render failure aborted the whole compare — now degrades to a row note + printed FAILED line, matching the failed-strategy-run policy. 14.3 docs spot-checked: sections present, snippets verbatim vs source | 14.1 + 14.3 accepted |
| 2026-07-16 | 13.3 visual review PASSED (fable-5, independent synthetic-harness screenshot): rep table 1-based, loss borders, TIME column, ▶/■ toggle, clip loop live (playhead cycling inside rep 3 with decoded frames + in/out markers), row/segment highlight agreement. Worker's own screenshot discipline caught a real "--0.0%" formatting bug pre-commit — recipe is working. ffmpeg stream-copy args verified against real video (6.33–14.25s → 7.9s clip) | 13.3 accepted |
| 2026-07-16 | 13.2 visual review PASSED (fable-5): worker only verified "no chrome regression" (segments need a tracked run), so fable-5 built a throwaway synthetic-results harness (temp example injecting a sinusoidal 6-rep BarPath into AppState, deleted after) and screenshotted segments live: 6 blocks positioned correctly, selected rep darker fill + border, in/out markers, playhead, coexisting 13.5 cards/loss column all good. Code review: `filter_map` over `rep_frame_bounds` can't misalign indices (bounds fail all-or-nothing via VelocityError). Nit for 13.3: current per-rep grid's `#` column is 0-based, design is 1-based — fix when 13.3 rebuilds the table | 13.2 accepted; 1-based rep numbering folded into 13.3's brief |
| 2026-07-16 | 13.1 visual review (fable-5, Xvfb screenshot): worker's Live/Results pill was drawn *first* in the toolbar row inside a `right_to_left` layout, which claims all remaining width — every other toolbar button (theme, Open video…, Place Seed, Calibrate, Track) vanished. clippy/tests/launch-smoke all green with the bug present — third confirmed case of non-visual "good" verdicts being false. Fixed inline: pill moved to end of row (comment in toolbar.rs records the trap). Rest of 13.1 verified visually: section labels, monospace `·` status bar, pill styling all match design | Fixed inline by fable-5; rule reaffirmed — every UI-touching task gets a screenshot check before acceptance |
| 2026-07-16 | Audit gate (milestone 10): pass — core dep-free, clippy 0, 315 tests, no-arg + arg smoke clean. During the milestone fable-5 review found 10.9 (reseed duplicate frames silently killing velocity/reps export — v1 reps.csv was header-only) and the 10.2 reacquire regression (fixed by 10.2b tune to 0.5 + distance guard). GUI app/ modules now 3170 lines total but split across 9 files — healthy. Versioning note: tags are sequential releases, not milestone numbers, from here (v0.6.0 = milestones 6+9+10) | Tagged v0.6.0. User should re-run GUI session + smoke checklist |

## Architecture (agreed)

Cargo workspace:
- `crates/tracker-core` — pure domain, no UI/IO deps. Geometry, trackers (Color + Template/ZNCC), gaps, calibration, kinematics, reps, color advisor.
- `crates/tracker-app` — adapters: ffmpeg subprocess IO, egui UI, overlay render, CSV/JSON export.

## Milestone 1 — core domain: template tracking

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 1.1 | Workspace scaffold: two crates, CI-able `cargo test` passes empty | S | done (sonnet-5, 2026-07-14) | |
| 1.2 | Geometry: `Point`, `Frame` (owned RGB buffer), pixel access | S | done (sonnet-5, 2026-07-14) | |
| 1.3 | Grayscale patch extraction with bounds handling | S | done (sonnet-5, 2026-07-14) | |
| 1.4 | ZNCC metric over two patches | S | done (sonnet-5, 2026-07-14) | `CorrelationMetric` trait + `Zncc` in `metric.rs`; mismatched sizes → `None`, zero-variance patch → `Some(0.0)` (no NaN) |
| 1.5 | Template Tracker: search window around last position, best-match step | M | done (sonnet-5, 2026-07-14) | `TemplateTracker` in `tracker.rs`; `TemplateTrackerConfig::builder()` (patch_radius/search_radius/min_score); `step` returns `StepOutcome::{Found{position,score}, Miss}` for 1.6's Gap logic |
| 1.6 | Gap logic: miss threshold, coast (hold/extrapolate), reacquire, `Gap` spans in path | M | done (sonnet-5, 2026-07-14) | `TrackingSession` in `session.rs`; wraps `TemplateTracker`, `step(frame)` per frame; `Sample{frame_index,position,source:Tracked\|Interpolated}` + `Gap{start,end}` spans; coast_limit (builder) misses before `SessionState::NeedsReseed` pauses `step`; `reseed(frame_index, point)` resumes and closes trailing gap |
| 1.7 | `BarPath` aggregate: positions + gaps + timebase (per-video fps) | S | done (sonnet-5, 2026-07-14) | `Timebase` (rational fps, num/den) in `bar_path.rs`; rejects zero num/den; `BarPath::new(&[Sample], &[Gap], Timebase, start_frame)` builds `PathPoint{frame_index,t_seconds,position,source}` list, video-absolute frame indices; `duration_seconds()`, `position_at(frame_index)` |

## Milestone 2 — video IO + UI shell

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 2.1 | ffprobe metadata adapter (w, h, fps incl. odd rates like 600/19, frame count) | S | done (sonnet-5, 2026-07-14) | `ffprobe::{VideoMetadata, ProbeError, parse_ffprobe_json, probe}` in tracker-app; fps kept as rational (num/den) for caller to build `Timebase`; `nb_frames` absent → `frame_count: None`; parsing unit-tested on canned JSON (serde/serde_json, tracker-app only); `#[ignore]`d integration test hits a real test_videos/ file (space-containing filename) — ran manually, passed: 1024x576, 600/19, 1910 frames |
| 2.2 | ffmpeg decode adapter: rawvideo pipe → `Frame` iterator (`FrameSource` port) | M | done (sonnet-5, 2026-07-14) | `FrameSource` trait (dependency-free, streaming `next_frame`) in tracker-core's `frame_source.rs`; `FfmpegFrameSource<R: Read>` in tracker-app's `ffmpeg_source.rs` — generic chunk reader unit-tested against `io::Cursor`/a trickling reader, `spawn()` wires it to a real `ffmpeg -f rawvideo -pix_fmt rgb24 -` child; partial trailing bytes at EOF are a documented `ShortRead` error (not silently dropped); child killed/reaped on `Drop`; `next_frame_checked` reaps exit status and surfaces stderr on non-zero exit. `#[ignore]`d integration test decodes first 10 frames of a real test_videos/ file (space-containing filename) — ran manually, passed at 1024x576. |
| 2.3 | egui app shell: open video, show frame, scrub bar | M | done (sonnet-5 + fable-5, 2026-07-15) | Seek-based decode (`ffmpeg -ss` per frame) + 16-frame LRU cache (~27MB) instead of full preload (~3.4GB). Worker stalled on Anthropic classifier outage; verification/commit finished by fable-5. |
| 2.4 | Seed placement: click → image-pixel coords (zoom-aware) | S | done (sonnet-5, 2026-07-15) | New `screen_map.rs`: pure `screen_to_image_px`/`fitted_image_rect` (letterbox offset + scale, capped at 1.0, out-of-image click → `None`), unit-tested without egui `Context`. `Mode::PlacingSeed` (toolbar toggle button + `S` key) added alongside `ViewOnly`; `AppState::place_seed`/`status_line` store `Seed{position: tracker_core::Point, frame_index}` and report it in the status bar. Crosshair drawn via `ui.painter()` overlay (`draw_seed_crosshair`), scaled back to screen coords from the same drawn image rect — no pixel mutation. |
| 2.5 | Calibration UI: two clicks + known length (450mm default) → px/m | S | done (sonnet-5, 2026-07-15) | `Calibration` value type (TDD'd first, no egui/UI deps) in tracker-core's new `calibration.rs`: `Calibration::new(a, b, known_length_meters)` rejects non-positive length and coincident points, exposes `px_per_meter()`/`px_to_meters()`. `Mode::Calibrating{first_point, known_length_meters}` added to app.rs (toolbar button + `C` key), reusing `screen_to_image_px`; `DragValue` field defaults to `DEFAULT_CALIBRATION_LENGTH_METERS` (0.450). Two clicks resolve into `AppState.calibration`; a third click restarts the pair (regardless of prior success/failure); coincident-point errors surface in the status bar, not a panic. Painter overlay draws the pending first point and the resolved segment line; status bar reports px/m. |
| 2.6 | Run tracking from UI, live crosshair + status bar (errors surface there, no unwrap) | M | done (sonnet-5, 2026-07-15) | New `tracking.rs` in tracker-app: pure `TrackingRunState` reducer (TDD'd) over `TrackingMessage{Progress,Done,Error}` sent from a background `std::thread` running `TemplateTracker`+`TrackingSession` fed by a single sequential `FfmpegFrameSource` (not the seek-based per-frame decoder — far faster). To start at the seed's frame, the worker decodes-and-discards sequentially up to `seed_frame_index` rather than an input-side `-ss` before `-i`, since that form of seek can land on the nearest keyframe rather than the exact frame for these odd frame rates (e.g. 600/19) and the seed must match what the user clicked. `NeedsReseed` blocks the worker on a `Receiver<ReseedCommand>` until the UI's `Resume` sends one (built from `TrackingSession::reseed`, already there from 1.6); UI auto-switches into `PlacingSeed` mode on pause so the user can click straight away. `AppState` gained `tracking`/`tracking_run`/`bar_path`; `poll_tracking` drains the channel each frame, advances `current_frame` to follow progress, and stores the finished `BarPath` (timebase from ffprobe's rational fps) for milestone 3. Default tuning consts (`DEFAULT_PATCH_RADIUS=12`, `DEFAULT_SEARCH_RADIUS=30`, `DEFAULT_MIN_SCORE=0.4`, `DEFAULT_COAST_LIMIT=5`) exposed for 3.4 retuning. Live crosshair (green) reuses the seed's crosshair painter, generalized to take a color. |

## Milestone 3 — outputs (works on test_videos/)

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 3.1 | Overlay renderer: path polyline + current point + legend onto frames | M | done (sonnet-5, 2026-07-15) | `overlay.rs` in tracker-core (pixel math on domain types, no UI dep, and 3.2's MP4 encode needs the same burned-in pixels egui can't provide); `OverlayStyle::builder()` (colors/thickness/marker radius/`show_legend`) mirrors `TemplateTrackerConfig`'s builder convention. `Frame::set_pixel` added to geometry.rs (bounds-checked, silently no-ops out of range — never panics). Bresenham line with square-stamp thickness; segments touching an `Interpolated` point render in `gap_color` instead of `path_color`. Legend is swatches-only, no text/bitmap font (documented in `OverlayStyle`'s doc comment) — a future task can composite real text once a text-rendering adapter exists. `render_overlay` filters `BarPath::points()` to `frame_index <= current_frame_index`, so it naturally supports being called per-frame while encoding (3.2). |
| 3.2 | ffmpeg encode adapter: frames → MP4 (`VideoSink` port) | S | done (sonnet-5, 2026-07-15) | `VideoSink` trait (dependency-free) in tracker-core's new `video_sink.rs`: `write_frame(&mut self, &Frame)` + `finish(self)` consuming to finalize, mirroring `FrameSource`'s port style. `FfmpegVideoSink<W: Write>` in tracker-app's new `ffmpeg_sink.rs` — generic writer logic unit-tested against `Vec<u8>` (exact byte count/content per frame, dimension mismatch → `DimensionMismatch` error, `finish` with zero frames succeeds); `spawn()` wires it to a real `ffmpeg -f rawvideo -pix_fmt rgb24 -s WxH -r <fps> -i - -c:v libx264 -pix_fmt yuv420p -movflags +faststart <out>` child, fps passed as `"num/den"` string (not collapsed to float) to preserve odd rates like 600/19. `finish` drops stdin (EOF signal) then waits on the child, surfacing non-zero exit + captured stderr; `Drop` kills/reaps an unfinished child so an early error path never leaves a dangling ffmpeg process. `#[ignore]`d integration test encodes 30 synthetic frames (moving 8x8 square) to a scratch MP4 and shells out to `ffprobe -count_frames` to confirm dims (64x48) and frame count (30) round-trip — ran manually, passed. |
| 3.3 | CSV/JSON export: `t, x_px, y_px, x_m, y_m, gap_flag` | S | done (sonnet-5, 2026-07-15) | `export.rs` in tracker-core, hand-rolled (no `csv`/`serde_json` dep) — output is a flat few-column table, trivial to string-build, keeps tracker-core dependency-free per the crate split. `export_csv`/`export_json(path: &BarPath, cal: Option<&Calibration>)`; columns `frame_index,t_seconds,x_px,y_px,x_m,y_m,gap_flag`. `x_m`/`y_m` = pixel coord × `Calibration::px_to_meters` — relative distances in the image's own origin (top-left, y down), not an absolute world position; empty (CSV) / `null` (JSON) without calibration. `gap_flag` = `source == Source::Interpolated`. Floats formatted `{:.6}`. JSON validity/shape verified in tracker-app's new `tests/export_json_validity.rs` by parsing with `serde_json` (already a tracker-app dependency) rather than string assertions alone. |
| 3.4 | End-to-end run on each video in `test_videos/`; record results here | M | done (sonnet-5, 2026-07-15) | Added headless `tracker-app track <video> --seed-frame N --seed X,Y --out <dir>` CLI mode (new `cli.rs`, dispatched from `main.rs`; GUI stays default with no subcommand) reusing 2.6's tracking pipeline, 3.1's `render_overlay`, 3.2's `FfmpegVideoSink`, 3.3's CSV/JSON export — no GUI needed. Headless has no human to reseed on `NeedsReseed`, so it auto-resumes from the last tracked position and counts these as reseed events (worst-case estimate of GUI intervention). Ran all 4 `test_videos/` clips; full per-video table in [docs/e2e-results.md](docs/e2e-results.md). Summary: 3 of 4 videos tracked cleanly with the first seed guess (0-1 reseed events, ≤0.3% interpolated points); the 4th needed a seed retune (260,120 → 300,150) to go from 77 reseed events to 0 — seed placement, not tracker tuning, was the dominant factor. Default 2.6 tracker config (patch/search radius, min_score, coast_limit) worked unchanged across all four. GUI Export button skipped (would grow this task past M; tracked as future work if wanted). Outputs written to `out/` (gitignored, not committed). |
| 3.5 | Handle rotation metadata; re-run e2e | M | done (sonnet-5, 2026-07-15) | Visual review (fable-5) found 3.4's v1/v2 results invalid: those clips carry a Display Matrix `rotation=-90` (`ffprobe`'s `stream_side_data`, coded 1024x576), and ffmpeg's decoder auto-applies that rotation to rawvideo output (576x1024 actual), but every frame buffer in the pipeline was sized from ffprobe's coded `width`/`height` — every decoded row was reinterpreted at the wrong stride, scrambling pixels (tracker was "tracking" garbage; CSVs still looked numerically plausible by coincidence). `ffprobe.rs`'s `VideoMetadata` now also parses `stream_side_data=rotation` (TDD'd against canned JSON: absent, -90, 90, 180, side-data-present-without-rotation-key) and exposes `display_width()`/`display_height()` (swapped from coded dims when rotation is an odd multiple of 90°). Every consumer that sizes decoded frame buffers — CLI `track` pipeline (`cli.rs`), GUI's `SeekingFrameDecoder` + tracking worker spawn (`app.rs`) — switched from `metadata.width/height` to `metadata.display_width()/display_height()`. v3/v4 have no rotation side data (already portrait-coded) so were unaffected; re-ran all 4 videos, re-picked v1/v2 seeds by extracting and visually inspecting portrait-oriented frames (v3/v4 seeds unchanged). Full updated results and takeaways in [docs/e2e-results.md](docs/e2e-results.md); verified by visually inspecting extracted overlay-MP4 frames (rack text upright, plate end-face round, not scrambled), not just CSV plausibility — that distinction is what caught this bug in the first place. |
| 3.6 | Adaptive dual-template tracking (anchor + adaptive template, update_threshold; CLI tuning flags); re-run e2e with visually-picked seeds | M | done (sonnet-5, 2026-07-15) | Motivated by fable-5 review: fixed seed template drifts on real footage (plate rotation, lighting). Dual matching: max(anchor score, adaptive score); adaptive template replaced only when winning score ≥ update_threshold (~0.7) to prevent drift-by-creep. TDD'd with a synthetic per-pixel pattern blend (not a brightness ramp — ZNCC is affine-invariant, so uniform brightness change wouldn't exercise the adaptive path). `track` CLI gained `--patch-radius/--search-radius/--min-score/--update-threshold/--coast-limit`. Re-ran e2e on fable-5's visually-picked seeds (out/v1d..v4d): v3/v4 perfect (0 reseeds), v1 good (2 gaps, 0 reseeds), **v2 regressed on the numbers (6 reseed events, 26 interpolated)** — flagged honestly in docs/e2e-results.md as needing fable-5's visual check (CSV plausibility alone previously produced false verdicts twice), overlay frames handed off in scratchpad. |

| 3.7 | Refactor: `spawn_tracking` params struct (9 args → builder/struct, audit finding) | S | done | sonnet-5, 2026-07-15 |

## Milestone 4 — color tracking

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 4.1 | `ColorModel` learned from seed patch (HSV median ± tolerance) | S | done | sonnet-5, 2026-07-15 |
| 4.2 | Color Tracker: threshold + centroid within search window | M | done | sonnet-5, 2026-07-15 |
| 4.3 | Tracker auto-suggestion: seed color distinct from background? → Color, else Template | S | done | sonnet-5, 2026-07-15 |

## Milestone 5 — kinematics + reps

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 5.1 | Smoothing (moving average / Savitzky-Golay) over positions | S | done | sonnet-5, 2026-07-15 |
| 5.2 | Velocity series (m/s via Calibration); raw positions preserved in export | S | done | sonnet-5, 2026-07-15 — new `velocity.rs`: `velocity_series(points, smoothing_window, cal)` smooths via 5.1's `smooth_positions` then differentiates (central differences interior, one-sided at endpoints) producing `VelocitySample { frame_index, t_seconds, vx, vy, speed, unit, from_interpolated }`. Axis convention documented: image y grows downward, so bar moving up ⇒ negative `vy` (5.3 will rely on this). Unit: px/s without `Calibration`, m/s with (single unit per call, no dual fields — raw px positions already preserved untouched in `PathPoint`/export). `from_interpolated` flags samples whose differencing endpoints touch a `Source::Interpolated` point, so 5.4's metrics can exclude coasted-over Gaps (honest numbers per CONTEXT.md's "Gap" term). Errors: `TooFewPoints` (<2), `NonMonotonicTime` (non-increasing `t_seconds`), `Smoothing` (propagated). `export_csv`/`export_json` gained a third `Option<&[VelocitySample]>` param (all call sites in `cli.rs`/tests updated to pass `None`); new columns `vx,vy,speed,velocity_unit,velocity_interpolated` (empty/`null` when velocity omitted or a frame has no matching sample), joined to points by `frame_index`; existing `x_px/y_px/x_m/y_m/gap_flag` columns unchanged. Workspace: 116 tests green (8 new in `velocity.rs`, 4 new in `export.rs`), clippy clean, `cargo fmt --check` clean. |
| 5.3 | Rep segmentation from vertical velocity sign changes | M | done | sonnet-5, 2026-07-15 — new `rep.rs`: `segment_reps(velocity: &[VelocitySample], config: RepSegmentationConfig) -> Vec<Rep>` where `Rep { eccentric_start, bottom, concentric_end }` are indices into the `velocity` slice (not raw frame numbers; callers look up `velocity[idx].frame_index`). Per CONTEXT.md's Rep term and 5.2's documented axis convention: eccentric (descent) = `vy > 0`, concentric (ascent) = `vy < 0`. Algorithm: (1) label each sample `Descending`/`Ascending`/`Idle` via a dead-band on `|vy|` (`min_velocity`, dodges noise-around-zero false sign-changes); (2) group into runs, fold `Descending`/`Ascending` runs shorter than `min_phase_duration_seconds` back into `Idle` (rejects single/double-frame jitter spikes) and regroup so adjacent `Idle` merges; (3) scan runs for `Descending` optionally followed by one `Idle` (pause-at-bottom plateau, `bottom` = its midpoint) then `Ascending` — emits one `Rep`; a `Descending` run with no following `Ascending` (clip ends mid-descent) is dropped, not emitted. `RepSegmentationConfig` via `builder()`, defaults `min_velocity: 5.0` (px/s-tuned; documented to override for m/s Calibration data), `min_phase_duration_seconds: 0.15` (assumes ~30-60fps, 2-6s squat reps). Core, dependency-free, no unwrap/expect outside tests. 8 new tests: single clean rep, 3 reps, noise-only jitter (0 reps), descent-only (0 reps), pause at bottom (still 1 rep, bottom in plateau), short jitter spike rejected, too-few-samples, default-config values. Workspace: 124 tests green, clippy clean, `cargo fmt --check` clean. |
| 5.4 | Per-rep metrics: depth, peak/mean concentric velocity; in overlay + export | M | done (sonnet-5, 2026-07-15) | New `rep_metrics.rs`: `rep_metrics(rep: &Rep, velocity: &[VelocitySample], points: &[PathPoint], cal: Option<&Calibration>) -> RepMetrics` (`all_rep_metrics` maps over every `Rep`). Relies on `segment_reps`'/`velocity_series`' documented 1:1 index alignment between `velocity` and its source `points`. `RepMetrics { start_t, bottom_t, end_t, depth, peak_concentric_speed, mean_concentric_velocity, unit, excluded_interpolated_samples }`: `depth` = max y - min y over the whole rep span (`eccentric_start..=concentric_end`), scaled via `cal` to match `velocity`'s unit; concentric phase = `bottom..=concentric_end`. Two velocity definitions, deliberately different (documented in the module doc comment): `peak_concentric_speed` = max instantaneous `\|vy\|` over non-interpolated concentric samples; `mean_concentric_velocity` = concentric **displacement/duration** (the VBT industry-standard "mean velocity", computed directly from the `bottom`/`concentric_end` endpoint positions/timestamps, not an average of per-sample speeds which would be sampling-density-biased). Honest numbers: `excluded_interpolated_samples` counts concentric `VelocitySample`s with `from_interpolated: true`, excluded from `peak_concentric_speed`. Export: kept reps as a **separate file/grain** rather than a second CSV section or embedded JSON array — points export is one-row-per-frame, reps are a different (much coarser) grain, so mixing would mean ragged rows or repeated rep data; new `export_reps_csv`/`export_reps_json` (`rep_index,start_t,bottom_t,end_t,depth,peak,mean,unit`), written by the CLI to `<stem>.reps.csv`/`<stem>.reps.json` alongside the existing `<stem>.csv`/`<stem>.json`. CLI: `track` gained `--cal x1,y1,x2,y2 --cal-length-m <meters>` (a mismatched pair is a warning, not a hard error — best-effort); when present, `Calibration` flows into `velocity_series`/`export_csv`/`export_json`/`rep_metrics` for m/s and meters everywhere, and `RepSegmentationConfig`'s dead-band is overridden to 0.03 (m/s-scale) instead of the px/s-tuned default 5.0 (documented in `rep.rs` as needed for calibrated data, previously unapplied — without this override calibrated runs detected zero reps). Per-rep summary lines (`rep N: depth=...m peak=...m/s mean=...m/s (K interpolated sample(s) excluded)`) printed to stdout after tracking. Overlay: new `render_rep_bottoms(frame, path, reps, frame_indices, current_frame_index, style)` draws a small horizontal tick (no font rendering available, per 3.1's known limitation) at each rep's bottom position once reached; kept as a separate function from `render_overlay` rather than changing its signature, called once more per frame from the CLI's overlay-render loop. Sanity run (v3 video, seed-frame 300/260,120, placeholder `--cal 200,120,320,120 --cal-length-m 0.45`): 10 reps detected (reps 0 and 9 are walk-in/walk-out noise, depth <0.1m; reps 1-8 depth ~0.82-0.96m, peak 0.95-1.13 m/s, mean concentric velocity 0.58-0.72 m/s) — slightly above the typical 0.3-0.8 m/s squat range, expected given the placeholder (unmeasured) calibration guess rather than a tracker/metric bug. Workspace: 145 tests green (10 new in `rep_metrics.rs`, 5 new in `export.rs`, 2 new in `overlay.rs`, 2 new in `cli.rs`), clippy `-D warnings` clean, `cargo fmt --check` clean. |

## Milestone 6 — marker color advisor

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 6.1 | Hue histogram over sampled frames | S | done (sonnet-5, 2026-07-15) | `hue_histogram` in `tracker-core/src/color_advisor.rs`; 36 buckets, sat/value floors, stride sub-sampling. |
| 6.2 | Recommend top marker hues (max distance from scene hues), CLI/UI report | S | done (sonnet-5, 2026-07-15) | `recommend_marker_hues` in `tracker-core`; `tracker-app advise <video> [--top-n N]` CLI subcommand. |

## Milestone 7 — usability, docs & distribution

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 7.1 | RUNNING.md: how to run (dev: `cargo run`; user: built binary), install on own machine (`cargo install --path`), prerequisites (ffmpeg/ffprobe), and a **manual test script** — step-by-step checklist per feature (open video, scrub, seed, calibrate, track, reseed, export) with expected results, used to validate releases by hand | S | done (sonnet-5, 2026-07-15) | Wrote `RUNNING.md` at repo root: prerequisites, dev run (GUI + headless `track` w/ full flag table incl. tuning + `--cal`/`--cal-length-m`), `cargo test`, `RUST_LOG`/log location, end-user release-tarball instructions incl. macOS Gatekeeper/`xattr` caveat, `cargo install --path`, "give to others" pointer, and an 8-step manual test script (open→scrub→seed `S`→calibrate `C`→track→occlusion/reseed→CLI `track`→`./scripts/smoke-report.sh`) cross-checked against `cli.rs`, `app/toolbar.rs`, `app/state.rs` (`WorkflowStep`), and `telemetry.rs`. Linked from README's Requirements section. Observation for 7.3: milestone 9's release workflow (9.2) already covers tagged-build distribution (Linux/macOS artifacts); remaining 7.3 scope is narrower — cargo-dist/homebrew polish — leaving 7.3 as `todo`. |
| 7.2 | Side panel UI: use the empty space right of the video for (a) a compact usage guide (steps with current-step highlight), (b) live debug/status panel — tracker state, current score, seed/calibration values, last errors with timestamps. Clean, readable design (grouped sections, color-coded state) replacing the single cramped bottom line. Includes splitting app.rs (872 lines, audit finding) into view modules | M | done (sonnet-5, 2026-07-15) | `app.rs` (927 lines) split into `app/{mod,state,toolbar,bottom_bar,side_panel,video_panel}.rs`; public surface unchanged (`app::run`/`AppState` re-exported from `mod.rs`), `main.rs` untouched. New `egui::SidePanel::right` (`side_panel.rs`, 260px default, resizable) shown before the central panel so the video fills whatever's left instead of empty space. Guide section: 5-step list, current step derived purely by `AppState::current_step()` (`WorkflowStep` enum, TDD'd in `state.rs`) — no seed → step 2, seed placed → 3, run active/paused → 4, `bar_path` present → 5; done steps grayed, current step accent-colored. Status section: grouped video/seed/calibration/tracking key-value rows, tracking state color-coded (green tracking/complete, yellow paused, red error) plus frames processed, gap count (new `TrackingRunState.gap_count`, incremented once per pause transition, TDD'd in `tracking.rs`), last position. Events section: `VecDeque<AppEvent>` ring buffer (cap 8) fed from the same call sites that already emit `tracing` breadcrumbs (seed placed, calibration set/failed, track started/paused/resumed/done/error); no wall-clock dep in this crate, so timestamps are seconds-since-app-start rather than clock time. Bottom bar kept as a one-line summary per the design. Workspace: 185 tests green, clippy clean, smoke run on the WhatsApp test video clean startup (no panics). |
| 7.3 | Distribution: GitHub release workflow (tagged builds, `cargo-dist` or manual artifacts for Linux + macOS), install instructions for non-developers in README | M | todo | |

## Milestone 8 — observability (tracing, crash reports, LLM-debuggable logs)

Rust-idiomatic pluggable telemetry: `tracing` for instrumentation; `tracing-subscriber` `Layer` as the plug-in protocol (console layer now; JSON-lines file layer now; `sentry-tracing`/Datadog layer possible later without touching instrumented code). `tracker-core` stays dependency-free — only adapters instrument.

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 8.1 | Logging foundation: `tracing` + `tracing-subscriber` in tracker-app; console (pretty) layer + JSON-lines rolling file layer (`tracing-appender`) to `$XDG_STATE_HOME/image-tracker/logs/` (`directories` crate); log path printed at startup; RUST_LOG env filter | M | done | sonnet-5, 2026-07-15 |
| 8.2 | Instrument pipeline: spans for probe/decode/track/encode/export with fields (video, frame index, score, session state); errors + reseed/gap events logged; GUI actions (seed, calibrate, track start/stop) as breadcrumb events | S | done | sonnet-5, 2026-07-15 — `ffprobe::probe` span + info/error events; `tracking::run_tracking_worker` span (video/seed_frame/seed_x/seed_y) with run-started/NeedsReseed(warn)/reseed(info)/Done/Error events, per-frame progress at trace (not info, avoids flooding); `cli::run_track` span, export paths at info, headless auto-reseed at warn; `app.rs` breadcrumbs (seed placed, calibration set, track started, mode changes) at info, frame decode errors at error; ffmpeg spawn at debug, non-zero exit stderr at error. Smoke: CLI track on WhatsApp 22.55.51 video (seed-frame 300, seed 260,120) -> 2887 points, 0 gaps; JSON log verified |
| 8.3 | Crash capture: panic hook → panic message + backtrace through tracing to file before exit; startup banner logs version + OS + ffmpeg version | S | done (sonnet-5, 2026-07-15) | |

## Milestone 9 — CI/CD (GitHub Actions)

Decisions (grilled 2026-07-15): native runners, no cross-compilation — `ubuntu-latest` + `macos-latest` (arm64, free on public repos; mac binaries unsigned, users right-click-open). Releases on **tag push only** (`v*`, matches milestone tagging rule) — main commits get CI, not releases. Test tiers: every push = unit+clippy+build (Linux); main = + one-video e2e (v3) with CSV sanity assertions; tags = + full 4-video e2e + matrix build + release upload. GUI smoke stays manual (no display in CI) with committed reports; self-hosted runner deferred with security constraints.

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 9.1 | CI workflow: push/PR → `cargo test --workspace` + `clippy -D warnings` + build (ubuntu, ffmpeg via apt); main pushes → + e2e on v3 video (assert ≥2500 points, 0 reseeds from CSV) | M | done (sonnet-5, 2026-07-15) | `.github/workflows/ci.yml`: `test` job (fmt --check, clippy -D warnings, test, release build) on push+PR; `e2e` job gated to `main` pushes only, runs the CLI `track` on the 22.55.51 sample video and asserts ≥2500 CSV data rows and ≤10 `gap_flag=true` rows (small tolerance, not a hard 0). `cargo fmt` was not clean pre-existing — ran it repo-wide (separate commit) before adding the workflow. |
| 9.2 | Release workflow: tag `v*` → matrix build (ubuntu-latest, macos-latest) + full 4-video e2e on ubuntu + GitHub Release with binaries (tar.gz per platform) | M | done (sonnet-5, 2026-07-15) | `.github/workflows/release.yml`, mirroring `ci.yml`'s conventions (dtolnay/rust-toolchain, Swatinem/rust-cache, apt ffmpeg). `e2e-full` job (ubuntu) runs the CLI `track` on all 4 `test_videos/` clips using the current seeds from `docs/e2e-results.md` (post-3.5 rotation fix: v1 frame789 seed 310,280; v2 frame1200 seed 290,390; v3 frame300 seed 260,120; v4 frame300 seed 300,150) and asserts loose CSV row-count floors (v1≥1000, v2≥800, v3≥2500, v4≥3000) as a regression guard. `build` job (matrix ubuntu/macos, `needs: e2e-full`) does `cargo build --release -p tracker-app --target <triple>` and packages the binary + README.md + LICENSE into `image-tracker-<tag>-<target-triple>.tar.gz`, uploaded as a build artifact. `release` job (`needs: build`, `permissions: contents: write` scoped to this job only) downloads the tarballs and publishes them via `softprops/action-gh-release@v2` with `generate_release_notes: true` plus a body noting the ffmpeg runtime dependency and the unsigned-mac-binary caveat (right-click Open / `xattr -d com.apple.quarantine`). All jobs have `timeout-minutes`; YAML validated with `python3 -c "import yaml; yaml.safe_load(...)"`. No tag pushed — that's fable-5's job after review. |
| 9.3 | README badges: CI status, release version, license; link to latest smoke report | S | done (sonnet-5, 2026-07-15) | Added CI (github actions badge for `ci.yml` on `main`) and latest-release (shields.io `github/v/release`) badges alongside the existing MIT/Rust/status badges, banner layout unchanged. Status section now lists milestones 1-5, 7.2, 8 as done and links `docs/smoke/`; the "39 domain tests" claim (stale — that was `tracker-core` only) replaced with "200+ workspace tests" (actual: 229 at time of writing across `tracker-core`/`tracker-app`/doc-tests). |
| 9.4 | Manual smoke kit: `scripts/smoke-report.sh` (runs scriptable checks — CLI usage exit, quick track run, version banner, log file exists; prints GUI checklist; emits markdown) + `docs/smoke/` results log committed by user | S | done (sonnet-5, 2026-07-15) | `scripts/smoke-report.sh` (executable, `set -euo pipefail`): builds the release binary, then (1) asserts a no-arg run exits non-zero with a usage line, (2) runs the CLI `track` on the v3 test video (seed-frame 300, seed 260,120) into a temp dir and asserts the primary CSV (careful to exclude `*.reps.csv`, which sorts before the main CSV under `find`) has >= 2500 data rows, (3) parses the version banner + `logging to <dir>` line from stdout and confirms an `image-tracker.log*` file exists under that dir. Writes `docs/smoke/YYYY-MM-DD.md` with the three results auto-filled plus an unchecked GUI checklist (open/scrub/seed/calibrate/track/side-panel/pause-reseed-resume); re-running same day overwrites. `docs/smoke/README.md` explains the kit. Ran the script for real; `docs/smoke/2026-07-15.md` is committed with all 3 script checks passing and the GUI section left unchecked, noted "GUI items pending manual run (amadeu01)". |
| 9.5 | (Deferred) Self-hosted runner on user machine for GUI smoke via `workflow_dispatch`; hard requirements: dispatch/schedule-only triggers, outside-PR approval required, unprivileged runner user | M | todo | Public-repo self-hosted runner = fork-PR code-exec risk; do not start without the listed mitigations |

## Milestone 10 — v1 usability (from first real user session, grilled 2026-07-15)

Longer-horizon deferred items live in [ROADMAP.md](ROADMAP.md). Bugs first.

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 10.1 | BUG: tracking runs past video end (user saw "frame 1990" on a 1909-frame video) — worker must stop at decode EOF, emit Done, UI transitions to Review; frame counter never exceeds total | M | done (sonnet-5, 2026-07-15) | Root cause was two-fold: (1) real bug — `run_tracking_worker`'s loop, on hitting `NeedsReseed` and blocking on `reseed_rx.recv()`, `return`ed straight out on a closed channel (UI/CLI dropping its handle while paused) *without ever sending `Done`*, leaving `TrackingRunState` stuck `running` forever from the caller's side — decode-EOF (`Ok(None)`) itself was already handled correctly. Fixed by extracting the loop into a generic `run_tracking_loop<S: FrameSource, T: Tracker>` (now unit-testable against an in-memory source) that returns `Ok(())` on both clean EOF *and* reseed-channel-closed-while-paused, so the caller always falls through to build+send `Done` with whatever samples/gaps exist. (2) display bug — ffprobe's `nb_frames` (this crate's `metadata.frame_count`) is only an estimate for some containers/odd frame rates (this project's footage reports rates like `600/19`) and undercounted the video's true decodable length; `AppState::set_frame`'s clamp used that stale estimate, freezing the video panel while `tracking_run`'s (deliberately uncapped) status text kept climbing past it — exactly the "runs past video end" symptom. Fixed by growing `metadata.frame_count` to the max frame index tracking actually reports, so the displayed frame count tracks reality instead of a possibly-wrong probe estimate. 3 new regression tests in `tracking.rs` drive `run_tracking_loop` over an in-memory `FfmpegFrameSource`/`ScriptedTracker`: clean EOF with frame indices bounded by frames fed, paused-at-EOF with reseed channel closed ends cleanly (the actual bug), and reseed-then-real-EOF terminates without hanging |
| 10.2 | BUG: lost-marker wandering — during miss/coast the crosshair must freeze at last good position and gray out ("lost"); reacquisition after a miss streak requires score ≥ update_threshold (0.7), not min_score (0.4), to avoid locking onto rack/mirror | S | done (sonnet-5, 2026-07-15) | Root cause: two separate honesty gaps. (1) `TrackingSession::step` accepted *any* `StepOutcome::Found` — even a marginal one just above the tracker's loose `min_score` (0.4) — as reacquisition once a gap was open, so a rack/mirror/background clutter that happened to score in the 0.4–0.7 band could end a gap and become the new "tracked" object, exactly the screenshotted bug. Fixed with a new `TrackingSessionConfig::reacquire_min_score: Option<f64>` (builder method, `None` default = old behavior, unit-tested for backward compat): mid-gap (`open_gap_start.is_some()`), a `Found` scoring below this threshold is demoted to `Miss` in `TrackingSession::step` before the existing Found/Miss branch runs — normal non-gap stepping is untouched. App wiring (`tracking::session_config`) always sets it to the resolved `update_threshold` (default `DEFAULT_UPDATE_THRESHOLD` 0.7, or the `--update-threshold` override if set) — no separate CLI flag, it inherits. 4 new `tracker-core` tests (`session.rs`, driven by a scripted `Tracker` test double for precise score control): weak mid-gap match stays a gap, strong mid-gap match closes it, non-gap tracking is unaffected by the threshold, and `None` reproduces the old any-`Found`-reacquires behavior. (2) GUI dishonesty — the crosshair drew at `last_position`, which during a coasted gap is the *linearly interpolated* point marching toward wherever the gap eventually closes (or the paused position), i.e. visibly "wandering" through frames where the object is actually lost. Fixed by adding `TrackingRunState::last_tracked_position` (only updated on `Source::Tracked` progress messages, so it freezes through interpolated/coasting frames) and `TrackingRunState::is_searching()` (true when the last sample was `Interpolated` or the session is `NeedsReseed`); `video_panel::show` now draws the crosshair at the frozen `last_tracked_position` and grays it out (`is_searching`) instead of green, `status_line` adds an "object lost — searching…" phase distinct from the existing "paused… place a new seed" text, and the side panel's tracking-state row shows the same "object lost — searching…" label in gray. **e2e sanity, honestly worse on v1**: re-ran v1 (seed-frame 789, 312,430) and v3 (seed-frame 300, 260,120) against the 3.6 baseline (v1: 2 gaps/0 reseeds; v3: 0 gaps/0 reseeds). v3 unchanged (0 gaps/0 reseeds, 2887/2887 tracked). v1 got worse — 8 gaps/7 reseed events (was 2/0) — because the stricter gate now demotes marginal-but-genuine reacquisitions (e.g. the plate sliding back into a partially-occluded rack corner) to misses just as readily as it demotes false rack/mirror locks, and v1's clip apparently has more of the former than the screenshot's failure mode. This is the expected shape of the tradeoff (fewer false "found the wrong thing" episodes, more honest "still searching" episodes) rather than a regression bug, but it is a real cost worth watching — a future task could make `reacquire_min_score` configurable independently of `update_threshold`, or taper it back toward `min_score` the longer a gap stays open, if v1-style clips turn out to be common. fmt/clippy/test clean (154 tracker-core + tracker-app unit tests, workspace-wide). |
| 10.2b | Decouple + tune `reacquire_min_score`: keep the anti-rack/mirror protection but stop punishing genuine marginal reacquisitions that made v1 e2e regress to 8 gaps/7 reseeds (10.2 observation) | S | done (sonnet-5, 2026-07-16) | Added `DEFAULT_REACQUIRE_MIN_SCORE` (tracker-app `tracking.rs`) and `TrackerTuning::reacquire_min_score`/`--reacquire-min-score` CLI flag, decoupling `session_config`'s `reacquire_min_score` from `update_threshold` (it used to inherit `update_threshold` 1:1). Swept 0.5/0.55/0.6 on v1 (seed 789, 312,430): 0.5 → 8 gaps/6 reseeds, 0.55 → 11/9, 0.6 → 8/7; also checked 0.4/0.45 out of curiosity — 0.4 (= `min_score`) reproduces the pre-10.2 baseline (2/0) exactly since the gate becomes a no-op, rejected on principle despite no observed jump artifact on this clip. Picked **0.5**: smallest gaps+reseeds among the sanctioned sweep, with the "tracked-sample max jump" metric (distance between consecutive *non-interpolated* samples, which would spike if a lower threshold let a reacquisition lock onto something far off) identical (42.4px) across the entire 0.4–0.7 range — no jump-spike regression at any tested value on this clip. v3 (300, 260,120) stays 0 gaps/0 reseeds at every threshold; v2 (1200, 283,430) stays in the same ballpark (11–15 gaps/8–10 reseeds) — no blow-up. Also added a second, independent guard: `TrackingSessionConfig::max_reacquire_distance` (builder method, `None` default = disabled) demotes a mid-gap `Found` to `Miss` if it lands farther than this from the last tracked position, regardless of score — belt-and-suspenders against a confident-but-wrong far lock. Wired in the app to `2 * search_radius` (no separate CLI flag, derives from `--search-radius`). TDD'd in `tracker-core/src/session.rs` (5 new tests: far match demoted even at score 1.0, close match still reacquires, non-gap tracking unaffected, `None` reproduces old behavior, plus the existing reacquire-score tests updated/kept). Re-running v1/v2/v3 with the guard active produced byte-identical gap/reseed counts to the score-only sweep — on these clips the score gate was already the binding constraint, so the distance guard is currently inert insurance rather than the thing that moved the numbers. Full sweep table and reproduction commands in `docs/e2e-results.md`'s new 10.2b section. fmt/clippy/test clean (workspace-wide). |
| 10.9 | BUG: reseed produces duplicate frame indices → `velocity_series` errors (NonMonotonicTime) → CLI *silently* exports no velocity and zero reps (v1 reps.csv was header-only). Fix: (a) session `reseed` must not duplicate/regress sample frame indices; (b) headless auto-resume must advance past the stuck frame instead of reseeding the same index repeatedly; (c) velocity/rep failures must surface as errors/warnings in CLI output and GUI events, never silently produce empty exports; (d) auto-resume reseeds at coasted-garbage position (y=12, rack top) — reseed from last *tracked* position instead | M | done (sonnet-5, 2026-07-16) | **Root cause, single chain**: `run_tracking_loop`'s `Progress` messages reported `video_frame_index` from `session.samples().last().frame_index` — but samples are only pushed on `Found`/reseed frames (and, retroactively, for a gap's interpolated frames once it *closes*); while a gap is open-but-unresolved (including the pause itself), the last sample is stale, many frames behind the session's true current frame. The CLI's headless auto-resume trusted that stale value as "the frame to reseed at" (`run_state.last_frame_index`), so on every pause it handed `reseed` the *same* already-recorded frame index back — `TrackingSession::reseed` had no guard against this and pushed a fresh sample at that duplicate index, and also **rewound** `self.frame_index` backwards to it, so tracking never advanced and kept re-pausing at the same spot forever (the 851/852/852/852… symptom). The resulting duplicate-frame-index samples gave `velocity_series` a zero/negative `dt`, which it correctly rejected (`NonMonotonicTime`) — but the CLI's `.ok()` on that `Result` silently discarded the error, so the run "succeeded" with a header-only `reps.csv` and no diagnostic. Fix, four parts: **(a)** added `TrackingSession::frame_index()` (exposes the session's always-advancing current frame, distinct from the last sample) and hardened `reseed(frame_index, point)`: if `frame_index <=` the last recorded sample's, it now *replaces* that last sample in place (position/point updated, no new entry) instead of appending a duplicate/regressing one — samples stay strictly increasing by construction, self-healing on the next `step`. TDD'd first (`session.rs`): `reseeding_the_same_frame_twice_keeps_samples_strictly_increasing` (same-frame and earlier-frame reseeds both clamp, never regress) and `frame_index_advances_on_every_step_even_without_a_new_sample` (documents the staleness the whole bug hinged on) — both failed against the old code before the fix, pass after. **(b)+(d)** `run_tracking_loop` now reports `video_frame_index` from `session.frame_index()` (the true current frame) instead of the stale last sample; the CLI's auto-resume now resumes at that correct frame using `run_state.last_tracked_position` (10.2, only updated on `Source::Tracked`) rather than `last_position` (which during a gap is wherever linear interpolation toward coasted-garbage currently sits, e.g. y=12/rack-top) — satisfies (d) directly since (b)'s frame-index fix and (d)'s position fix were the same auto-resume call site. **(c)** `run_track` no longer swallows `velocity_series`'s `Result` via `.ok()`: an `Err` now prints an `eprintln!` warning plus a `tracing::error!`, and the final summary distinguishes "(no reps: velocity could not be computed — see warning above)" from the ordinary "(no reps detected)"; CSV/JSON exports already handled `velocity: None` gracefully (positions still written, velocity columns empty) so no export-path change was needed there. GUI seam (`state.rs`) deferred — that file doesn't exist yet (10.3 hasn't landed); left a note here for whoever lands 10.3 to store the velocity computation as a `Result` from the start rather than reproducing the CLI's original `.ok()` mistake. **e2e verification**: v1 (seed 789, 312,430) — was silently 0 velocity/0 reps (header-only reps.csv) — now produces 1192 points (1187 tracked/5 interpolated), 8 gaps, 6 reseed events (unchanged tracking behavior from 10.2b), and **13 reps detected** with real mean concentric velocities (e.g. rep 0: depth 425px, mean 298.2px/s; rep 3: depth 496px, mean 372.9px/s; rep 11: depth 76px, mean 6.3px/s). v3 (seed 300, 260,120) stays byte-identical to the 10.2b baseline: 2887 points, 0 gaps, 0 reseeds, 11 reps intact (rep 3: depth 251px, mean 185.9px/s). fmt/clippy/test clean workspace-wide (158 tracker-core + tracker-app unit tests). |
| 10.3 | Review state + Results: end of tracking → Review; side panel Results section (headline rep count, per-rep table depth/peak/mean, unit, quality warnings incl. gaps + interpolated counts); bar path polyline drawn on video panel; auto-export next to source video (`<video>.overlay.mp4`, `.csv`, `.json`, `.reps.csv`) with an "exports written" event listing paths | M | done (sonnet-5, 2026-07-16) | `AppState::poll_tracking`'s `Done` branch now builds a pure `SessionResults` (`app/state.rs`: `bar_path`, `velocity: Result<Vec<VelocitySample>, VelocityError>`, `reps`, `metrics`, `unit`, a `ResultsQuality` gaps/reseeds/interpolated-% summary) using the exact same `velocity_series`(smoothing window 5)/`segment_reps`/`all_rep_metrics` pipeline the CLI uses — the calibrated-vs-uncalibrated rep dead-band tuning was pulled out of `cli.rs` into a shared `tracking::rep_segmentation_config(calibrated)` so GUI and CLI runs of the same video/seed can't silently disagree on rep count. Per 10.9's noted GUI seam, a `VelocityError` is never swallowed: it's stored as `Err` on `results.velocity`, pushed as a `Warn` event ("velocity unavailable: …"), and shown in place of the Results table rather than producing an empty one. `WorkflowStep::Review` already existed (keyed off `bar_path.is_some()`) and needed no change — the guide already moves to step 5 the moment `results` exists; "mode" stays whatever click-mode it was (Review isn't a `Mode` variant, since `Mode` only governs click handling, not workflow phase) — the side panel/video panel gate their Review-only rendering on `state.results.is_some()` instead. Side panel: new `results_section` (`app/side_panel.rs`) — headline "Reps: N", an egui `Grid` per-rep table (depth/peak/mean, 2 decimals, unit-suffixed headers), and a Quality block (gaps, reseeds, interpolated N/total (%), colored amber above 20%). Video panel: new `draw_bar_path` (`app/video_panel.rs`) — polyline over `results.bar_path.points()`, green segments between two `Source::Tracked` points, orange if either endpoint is `Source::Interpolated`, plus a white crosshair at the current scrub frame's path point when one exists. Auto-export: the CLI's overlay render loop (`render_overlay_video`, previously private to `cli.rs`) was extracted verbatim into a new shared `overlay_export.rs` module; a new `export_job.rs` (`ExportJob`/`ExportHandle`/`ExportMessage`, mirroring `tracking.rs`'s worker-thread/channel shape) spawns a background thread on every successful `Done` that writes `<stem>.csv/.json/.reps.csv/.reps.json` next to the source video via the same `export_csv`/`export_json`/`export_reps_csv`/`export_reps_json` the CLI uses, then calls the shared overlay renderer for `<stem>.overlay.mp4`; each write sends `ExportMessage::Written(path)` (surfaced as an Info event), a failure sends `Error(String)` (Error event, non-fatal — the job keeps going), and the job always ends with `Done` (`AppState::poll_export`, wired into `TrackerApp::update` alongside `poll_tracking`, turns that into the "exports written" summary event and drops the handle; also requests repaints while `export.is_some()`). Pure logic TDD'd in `app/state.rs`: `SessionResults::build` (reps/units/quality on a synthetic one-rep path, both calibrated and uncalibrated; the `VelocityError` non-silent-failure case on a single-point path), `ResultsQuality::interpolated_percent`, and a `poll_tracking`-shaped integration test proving `Done` populates `results` and flips `current_step()` to `Review`; `export_job`'s `output_paths` naming is also unit-tested. fmt/clippy/test clean workspace-wide (156 tracker-core + 112 tracker-app unit tests + 2 export-JSON integration tests). Smoke: `cargo run -p tracker-app -- test_videos/...mp4` starts clean, no panics. |
| 10.4 | Session lifecycle controls: Start/Pause/Resume/Stop (keep results so far → Review)/Discard (trash run, back to seed placement); "New session" resets seed/calibration/results without restarting app | M | done (sonnet-5, 2026-07-16) | Worker gained a `ControlCommand{Pause,Resume,Stop}` channel (`tracking.rs`) alongside the existing reseed channel; `run_tracking_loop` checks it (non-blocking) before every frame read, so Stop/Discard terminate promptly rather than waiting for the current frame — Pause genuinely blocks frame consumption via a blocking `recv()`, not a GUI-side hold on a worker that keeps decoding underneath. Stop reuses the existing `Done`-with-partial-samples path (same as clean EOF) so it lands in Review with whatever was tracked so far; Discard sends the same `Stop` but the UI clears `tracking`/`tracking_run`/`bar_path`/`results` itself without waiting for that `Done` (the worker still exits promptly and its `FfmpegFrameSource` `Drop` still kills/reaps ffmpeg) and returns to `Mode::PlacingSeed` with the Seed intact. Toolbar (`toolbar.rs`) shows Pause/Resume + Stop + Discard while a run is active (swapped for Track), and Re-track/New session once in Review. New `AppState` methods (`state.rs`) — `pause_tracking`/`unpause_tracking`/`stop_tracking`/`discard_tracking`/`start_new_session`/`retrack` — each gated by a `can_*` predicate the toolbar uses for enablement, and each emits a `tracing` breadcrumb + `AppEvent`. New session clears seed/calibration/results/events and returns to step 1 on the same video (no restart); Re-track keeps seed+calibration, clears results, and calls `start_tracking()` immediately. TDD'd in `state.rs` (discard keeps seed, resets to PlacingSeed; new-session clears everything incl. events; re-track preserves seed/cal and starts a run) and `tracking.rs` (Stop ends early with zero frames read; Pause blocks until Resume, proven via a delayed-resume background thread; Stop-while-paused doesn't hang). `cargo fmt`/`clippy -D warnings`/`test` clean workspace-wide; smoke-verified the app opens a video cleanly. |
| 10.5 | Open video from UI + app identity: "Open video…" button (rfd file dialog) usable at startup (no CLI arg → empty state with open prompt) and any time; window title "Image Tracker"; Linux WM_CLASS/app_id set; `assets/image-tracker.desktop` + icon, install note in RUNNING.md | M | done (sonnet-5, 2026-07-16) | App shows as "unknown" in Linux launcher |
| 10.6 | Timeline: sampled thumbnail strip (~20 tiny frames decoded once in background, click to jump, position marker) above the slider; ←/→ = ±1 frame, Shift+←/→ = ±10; bigger prev/next buttons; tooltips on all controls | M | done (sonnet-5, 2026-07-16) | Full per-frame filmstrip infeasible (seek+decode per frame) — sampled strip agreed |
| 10.7 | Mode instruction banners + expandable guide: Calibrate shows step text ("click one plate edge → opposite edge → enter real length, competition plate = 0.450 m", live "1 of 2 points"); Seed/Track modes similar; side-panel guide steps expandable with 2-line how-tos | S | done (sonnet-5, 2026-07-16) | User: "app does not explain how to use calibrate" |
| 10.8 | Live phase display + live rep counter: status shows "tracking bar path… frame N/M" → "computing metrics…" → Review; during tracking side panel shows "reps so far: N" recomputed every ~30 frames from partial path | M | done (sonnet-5, 2026-07-16) | Makes path/velocity/rep stages visible; user: "can it simply count the repetition?" |

## Milestone 12 — second user session fixes (2026-07-16)

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 12.1 | BUG: "Open video…" panics on Wayland — rfd xdg-portal backend built with `tokio` feature but no tokio runtime ("there is no reactor running", zbus executor). Fix: rfd `async-std` feature (self-contained executor). Verify dialog opens on user's Wayland session; regression note in Cargo.toml comment | S | done (fable-5, 2026-07-16) | Found via panic-hook backtrace in user's log — telemetry paid off. Build+tests green; dialog needs user's Wayland session to confirm interactively |
| 12.5 | Per-rep cards | M | superseded | Absorbed into milestone 13 (13.2/13.3) after user imported the VBT Tracker UI design |
| 12.6 | Exports discoverability: Results section lists every written file as clickable rows (full path, copy button) + "Open folder" button (xdg-open/open); export events already exist — surface them persistently, not only in the events feed | S | done (sonnet-5, 2026-07-16) | New `AppState::exported_files: Vec<PathBuf>` fed straight from `poll_export`'s `ExportMessage::Written(path)` (the paths already flowed there, previously only as a transient event) — cleared in `start_export` (fresh run) and `start_new_session`, so it never shows a stale file from a previous run/video; deliberately *not* cleared on `retrack`, since `results` (which gates whether the Results section renders at all) is cleared there too, so the stale list is invisible until the new run's own `start_export` clears it anyway. New `files_section` in `side_panel.rs`, appended to the existing Results section: one row per file — filename with the full path as a hover tooltip, a "Copy path" button (`ui.output_mut(|o| o.copied_text = ...)`), and an "Open folder" button. `open_containing_folder` spawns the platform file manager on the path's parent directory (`open` on macOS, `explorer` on Windows, `xdg-open` elsewhere via `cfg(target_os = ...)`) with `Command::spawn` (never `status`/`output`, so the UI thread never blocks on it) and downgrades any spawn error to a `tracing::warn!` rather than propagating — no file manager registered, a headless/sandboxed box, etc. can't crash the app or freeze the click. fmt/clippy(`-D warnings`)/test clean workspace-wide; smoke: no-arg and video-arg GUI launches both verified running (timeout-killed, not crashed). |
| 12.7 | VBT graphs | M | superseded | Absorbed into milestone 13 (13.4/13.5) after user imported the VBT Tracker UI design; velocity-vs-time + 2D path plots remain candidates for 13.4 follow-up |
| 12.2 | Crash bundle: panic hook additionally writes self-contained `crash-<timestamp>.log` (startup banner, last ~50 events from a ring buffer shared with the GUI events feed, panic + backtrace) into the logs dir — single file to paste into an LLM for debugging; log path printed on crash to stderr | M | done (sonnet-5, 2026-07-16) | New `BreadcrumbLayer` (`telemetry.rs`): a `tracing_subscriber::Layer` (not a per-call-site API) so it captures every `info!`/`warn!`/`error!` event from every thread — worker threads (tracking/export/thumbnail/benchmark) included — into a global `Mutex<VecDeque<String>>` ring capped at `MAX_BREADCRUMBS=50`, formatted as one self-contained line (timestamp, level, target, message, fields) per event. `set_startup_banner`/`startup_banner` (`OnceLock<String>`) captures the version/os/arch/ffmpeg banner once in `main.rs` right after it's computed, so the panic hook never needs to re-spawn `ffmpeg -version`. Panic hook now also calls pure `crash_report(banner, breadcrumbs, message, location, backtrace)` and best-effort `write_crash_report` (`log_dir()/crash-<unix-ts>.log`); any I/O failure is swallowed (crash bundle writing must never mask the panic) and the path is printed to stderr on success. TDD: ring-capping eviction, `crash_report` formatting (with/without breadcrumbs), `write_crash_report` round-trip, and a `BreadcrumbLayer` capture test via `tracing::subscriber::with_default` (a real global subscriber can only be installed once per process, so this drives the layer directly rather than through `telemetry::init()`). Manual smoke: a temporary `examples/force_panic.rs` (deleted after) forced a panic and confirmed `crash-*.log` contains the banner, breadcrumbs, and full backtrace. |
| 12.3 | More breadcrumbs: file-dialog opened/picked/cancelled, video loaded (path, dims, fps, rotation), settings changes (strategy describe()), thumbnail worker start/done, export job start/done per file, benchmark start/done | S | done (sonnet-5, 2026-07-16) | `TrackerApp::prompt_open_video` (`app/mod.rs`) now logs "file dialog opened"/"file dialog picked path"/"file dialog cancelled". `AppState::note_video_opened` (`state.rs`) gained a `&VideoMetadata` param so both its `tracing::info!` and its `push_event` (user-visible) carry width/height/fps/rotation, not just the path — call site updated in `app/mod.rs`, tests updated. `start_strategy_benchmark` now also `tracing::info!`s the strategy count (`compare::strategy_matrix().len()`) alongside the existing `push_event`; the benchmark-done branch of `poll_benchmark` computes the winner via `compare::recommend` and includes its `label()` in both the tracing breadcrumb and the on-screen event. `compare::run_compare` (the CLI path) got matching start/done breadcrumbs with the winner label, so `tracker-app compare` breadcrumbs land in the ring even outside the GUI. `thumbnail_worker::spawn_thumbnails` logs start (sampled count) before spawning the thread and done (same count) at the end of the decode loop. `export_job::run_export`/`write_file` log "export job started" once, "export file done"/"export file failed" per file (CSV/JSON/reps/overlay all go through the same `write_file` or the overlay's own match arm), and "export job done" at the end — these were previously silent from `tracing`'s perspective (only reachable via `AppState::poll_export`'s `push_event`s, which don't cover worker-thread-only failures). fmt/clippy(`-D warnings`)/test clean workspace-wide (194 tracker-app + 173 tracker-core tests, up from 188/173); smoke: no-arg GUI launch clean (timeout-killed, not crashed); forced-panic smoke (temporary `examples/force_panic.rs`, deleted after) confirmed `crash-*.log` carries these new breadcrumbs alongside the panic. |
| 12.4 | Theme: light/dark toggle in toolbar (egui built-in visuals), follows system preference at startup where detectable, persisted via eframe storage; verify side panel/banner colors readable in both themes | M | done (sonnet-5, 2026-07-16) | Startup "follow system" needed no new code: `egui_winit::State` already reads the OS theme via `winit` and applies it to `egui::Context` before the first frame (confirmed by reading `eframe-0.29.1`'s `native/glow_integration.rs`/`wgpu_integration.rs`, which pass `event_loop.system_theme()` into `egui_winit::State::new`) — 0.29 has no `NativeOptions::follow_system_theme`/`default_theme` field (that's a later eframe), the behavior is just always-on. New toolbar sun/moon button (`toolbar.rs`, reads `ctx.style().visuals.dark_mode` for its icon/tooltip) calls `TrackerApp::toggle_theme` (`app/mod.rs`), which flips `egui::Visuals::dark()`/`light()` immediately via `ctx.set_visuals` and records the choice on a new `theme_override: Option<bool>` field. Persistence skipped eframe's `persistence` feature (a whole `App::save`/RON-blob mechanism for one boolean) in favor of a standalone `app/theme.rs`: a tiny `theme.json` written next to `telemetry::log_dir()` (same `directories::ProjectDirs` triple, already a dependency) via `load_override`/`save_override`, both best-effort — a missing/corrupt file or write failure just falls back to "follow system" / logs a warning, never panics. `update()` re-applies a persisted override only when it's actually drifted from the current `dark_mode` (a one-time correction right after launch — nothing else changes `dark_mode` behind the override's back), so an unset override (`None`, the common case) never fights egui/winit's own `ThemeChanged` handling. New `app/palette.rs`: every previously-hardcoded status/severity `Color32` (banner backgrounds+text, the tracking-state green/yellow/red row, the events feed, the benchmark winner highlight, the guide's current-step/done markers) now goes through `palette::status_color(dark_mode, StatusKind)`/`palette::banner_colors(dark_mode, BannerKind)` instead — two independent palettes (bright/saturated for dark panels, deep/desaturated for light ones), not one shared compromise color, because e.g. the old warn yellow `(230,200,60)` had under 1.6:1 contrast against a white panel. TDD: colors differ per theme, and a WCAG-style `relative_luminance`/`contrast_ratio` pair (only compiled under `#[cfg(test)]`) asserts every status color clears 3:1 (AA large-text) against egui's actual default dark/light panel fill, and every banner background/text pair clears 4.5:1. Video-panel/thumbnail overlays deliberately left alone: those draw on top of decoded video pixels, not the app's panel chrome, so the theme toggle doesn't change their contrast story. fmt/clippy(`-D warnings`)/test clean workspace-wide (200 tracker-app tests, up from 194); smoke: no-arg and video-arg GUI launches both verified running (timeout-killed, not crashed). |

## Milestone 13 — VBT Tracker UI design implementation (imported 2026-07-16)

Source of truth: [docs/design/vbt-tracker-ui.dc.html](docs/design/vbt-tracker-ui.dc.html) (interactive mock from user's Claude Design project) + [docs/design/vbt-tracker-ui-notes.md](docs/design/vbt-tracker-ui-notes.md) (element→task mapping, palette translation, deliberate deviations). Supersedes 12.5 and 12.7.

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 13.1 | Shell restyle per design: Live/Results pill toggle (toolbar right), hint bar styling, status bar (monospace one-liner), section-label typography (uppercase small labels), design palette mapped through palette.rs for both themes | M | done (worker, 2026-07-16) | `palette.rs`: new `ChromePalette{app_bg,panel_bg,border,accent}` + `chrome_palette(dark_mode)`, dark values the design's exact hex (`#141416`/`#1f1f24`/`#2c2c31`/`#6ea3ec`), light values freshly picked (not lightened dark hex) so the accent stays ≥3:1 on a white panel — 5 new tests (differ-between-themes, dark-hex-matches-mock, accent-contrast, panel/border-distinguishable), following the `LossSeverity`/`loss_severity_color` convention from 13.5 rather than inventing a new one. `state.rs`: new `DisplayMode{Live,Results}` + `AppState.display_mode` (defaults `Results`); `start_tracking` flips it to `Live`, the `poll_tracking` results-ready branch flips it back to `Results` — pure state, no dedicated Live layout yet (that's 13.6; Live currently just reuses the existing Status section's live rep counter). `toolbar.rs`: `display_mode_pill` renders the Live/Results `selectable_label` pair right-aligned via a nested `Layout::right_to_left` inside the toolbar's horizontal, with a sine-wave-alpha pulsing dot (via `ctx.request_repaint()`) next to Live when selected, colored with `chrome_palette`'s accent. `banner.rs`: kept the existing (already contrast-tested) `banner_colors` background/text pairs — translated the design's "hint bar" look via a hairline `chrome_palette` border stroke and a smaller (12.5pt) point size rather than recoloring the background to the raw design hex, since white/black text over the exact `#6ea3ec` etc. fails contrast in one theme (documented deviation, same reasoning as `loss_severity_color`). `bottom_bar.rs`: `show_status_bar` now renders every field (path/frame/status_line/tracking status/error) through `egui::RichText::monospace()` and adds a `chrome_palette` border-colored top hairline via a custom `Frame`; `·` replaces `|` as the field separator to match the design copy. `side_panel.rs`: new `section_label(ui, text)` helper — uppercase text joined with U+2009 thin spaces to approximate the design's ~11px letter-spacing (egui has no letter-spacing primitive), 11px size, `StatusKind::Neutral` color — replaces the 4 section-header `ui.heading()` calls (Guide ×2, Status, Results, Events); left the one unrelated `.heading()` (the "Reps: N" headline value in `results_section`) untouched. fmt/clippy(`-D warnings`)/test clean workspace-wide (211 tracker-app + 181 tracker-core tests, 4 new palette tests included); smoke: no-arg and video-arg (`test_videos/…14.03.30.mp4`) GUI launches both verified running via `timeout`. |
| 13.2 | Rep segments on scrub bar: clickable block per Rep positioned by start/end frames, selected highlight, in/out markers when a rep clip is active; rep selection state on AppState (drives table row highlight + video path segment highlight) | M | done (worker, 2026-07-16) | `state.rs`: `SessionResults::rep_frame_bounds(index) -> Option<(u64,u64)>` — `Rep`'s fields are *indices into the velocity slice*, not frames, so bounds resolve through the velocity samples' `frame_index` (already carries the session `start_frame` offset); `None` on out-of-range or `VelocityError`. New shared selection state `AppState.selected_rep: Option<usize>` + `AppState.rep_clip: Option<usize>` (clip stays unarmed until 13.3's ▶; distinct fields per the mock — `onSelect` sets selection and *clears* clip). `select_rep(i)`: select + jump playhead to rep start via `set_frame` + clear clip; no-op on stale/out-of-range index. Both fields cleared everywhere `results` is cleared (`start_tracking`, `discard_tracking`, `start_new_session`, `retrack`) — 5 new state tests. `bottom_bar.rs`: custom segment strip per the design-notes mapping — `allocate_exact_size(34px, Sense::click())`, `painter` rect per rep, fills `chrome_palette().accent.gamma_multiply(0.45)` selected / `(0.16)` unselected, 2px playhead vline (`strong_text_color`), 2px accent in/out vlines when `rep_clip` is armed, "Rep N" hover tooltip; renders only when results have reps, existing Slider row kept below for fine scrub (sanctioned deviation). Click on empty track = coarse seek. Geometry/hit-test kept as pure fns `segment_fraction` (mock's `leftPct`/`widthPct` math, clamped, zero-total safe) and `segment_at` (inclusive ends, earlier rep wins shared boundary) — 4 new tests; painter code stays thin/untested. fmt/clippy(`-D warnings`)/test clean workspace-wide (220 tracker-app + 181 tracker-core). Visual check (Xvfb + `import` screenshot, video arg): full chrome intact — toolbar buttons, Live/Results pill, thumbnails, slider, status bar all render; segments correctly absent pre-tracking (rendering segments live needs a tracked run — not exercised, geometry covered by unit tests instead). |
| 13.3 | Rep table (Results): #, depth, peak, mean, loss %, time range, ▶ button; loss-colored left border + values (green/amber/red vs threshold/2, threshold); row click → jump; ▶ → in-app rep loop (playhead cycles in/out via seek decoder) + "Export all rep clips" via ffmpeg stream copy (`video.repNN.mp4`, -ss/-to -c copy) | M | done (worker, 2026-07-16) | `side_panel.rs`: 10.3's bare `egui::Grid` replaced by the design's rep table, built per the design-notes egui mapping — per row one `allocate_exact_size(22px, Sense::click())` rect, painted 3px left-border strip in `palette::loss_severity_color` (rep 1's `None` loss counts as 0% → green, matching the mock), monospace `painter.text` at fixed x-offsets (`REP_COL_X`), header labels at the same offsets, right-aligned ▶ via `ui.put(rect, Button)`; ▶ response checked *before* the row response (the mock's `stopPropagation`) so playing never also row-selects-and-clears-clip; ▶ shows ■ while its clip is armed. Rep numbers now 1-based (13.2 review nit fixed). Row highlight reads the same `selected_rep` the scrub segments do. `PANEL_WIDTH` 260→320 (7 monospace columns need ~300px; still user-resizable). Uncalibrated values render as whole px (mock's `fmtV`); loss clamped `max(0.0)` for display (a rep faster than rep 1 printed "--0.0%" otherwise). `state.rs`: `toggle_rep_clip(i)` (arm=select+jump to start, second ▶ disarms leaving playhead/selection), `advance_rep_clip()` (steps playhead one frame, wraps end→start via pure `clip_loop_next_frame` — also snaps back in if the user scrubbed outside the clip), `format_clip_time` (mock's `fmtTime`, M:SS.s, degenerate-fps safe), `can_export_rep_clips`/`start_rep_clip_export`. Clip loop is the design notes' sanctioned deviation: playhead cycling through the existing seek decoder, not true decoded playback — `TrackerApp::update` calls `advance_rep_clip` then `ctx.request_repaint_after(frame_duration)` so it cycles at ~video fps (`ensure_texture` decodes each new frame). `export_job.rs`: `RepClipJob`/`spawn_rep_clip_export` — background thread, one `ffmpeg -y -ss <start> -to <end> -i <video> -c copy <stem>.repNN.mp4` per rep (input-seeked stream copy; keyframe-snapped cuts accepted v1), NN 1-based zero-padded, reporting per-file `Written`/`Error` + `Done` over the *same* `ExportMessage` channel auto-export uses, so `poll_export`/events/`exported_files`/Files list all work unchanged; shares `self.export` (one export at a time, gate in `can_export_rep_clips`), and unlike `start_export` does NOT clear `exported_files` (auto-export's files stay listed). Pure fns TDD'd: `clip_loop_next_frame` (wrap/snap), `format_clip_time` (incl. 600/19 fps), `frame_time_seconds`/`clip_time_range` (degenerate fps → 0), `rep_clip_output_path` (`video.rep01.mp4`), `rep_clip_ffmpeg_args` (exact arg vector) + state tests for toggle/advance/export gate — 11 new tests (230 tracker-app + 181 tracker-core, fmt/clippy `-D warnings` clean). Visual check (fable-5 recipe: temp `preview_results.rs` example injecting a synthetic 6-rep sinusoidal path into the real test video, Xvfb + `import`, harness+temporary `SessionResults` re-export both reverted before commit): table renders 1-based #, green 3px left borders, LOSS/TIME columns, ▶ per row, `selected_rep=2` row highlight matches scrub segment 3's darker fill; toolbar/banner/thumbnails/segments/slider/status bar unregressed. First screenshot caught the "--0.0%" double-minus (fixed as above). ffmpeg clip command sanity-run against the real video: 6.33–14.25s range → 7.9s stream-copied clip. |
| 13.4 | Velocity chart (Results): mean concentric velocity by rep — line + loss-colored clickable dots, dashed −10/−20/−30% threshold lines vs rep 1, linear trend line, y-axis ticks (unit-aware); egui painter or egui_plot | M | done (worker, 2026-07-17) | Absorbs 12.7. Hand-painted per the design notes (no egui_plot dep): `velocity_chart` in `side_panel.rs`, drawn below the rep table inside a chrome-palette card (panel_bg fill / border stroke / 6.0 rounding), header "MEAN CONCENTRIC VELOCITY BY REP" + right-aligned "dashed: 10 / 20 / 30% loss" hint. Geometry is the mock's SVG verbatim, generalized to the allocated rect: insets 34px left / 30px bottom / 10px top+right, 24px x-padding before first/after last dot (`chart_x`/`chart_y`/`chart_value_range` pure fns, 8 unit tests — endpoints hand-checked against the mock's `xOf`/`yOf` at viewBox scale); height = width × 190/420 clamped 140–200 so the resizable panel keeps the mock's aspect. y-range = data min/max ∪ the three threshold values, padded 5% (flat/degenerate data gets an artificial span rather than a divide-by-zero). Trend = `tracker_core::linear_trend(values) -> Option<(slope, intercept)>` in `rep_metrics.rs` next to `velocity_loss_percent` (TDD, 5 tests: exact-line recovery, flat→0 slope, mock-formula parity on the mock's 8 means, None for <2 points / non-finite values), drawn as a dashed accent line; dashed threshold lines at `v1·(1−0.1/0.2/0.3)` in fixed green/amber/red (`LossSeverity` colors, only when rep 1's mean is a finite positive baseline). Dots `circle_filled` colored by `loss_severity` vs the user threshold (rep 1 counts as 0% like the table), selected r 6 vs 4.5, panel-bg ring; hover within r+2 shows "Rep N — 0.82 m/s (−12%)" tooltip; click hits-tests by distance and calls `select_rep` (same shared selection as table rows/scrub segments). y ticks unit-aware (m/s 2 decimals, px/s whole — the mock's `fmtV`), x labels 1-based rep numbers. Visual check (Xvfb + synthetic 6-rep decaying-sinusoid harness, since reverted): card + line + dots (green/amber/red spread) + 3 dashed thresholds w/ labels + axis ticks all render; selected rep 3's dot visibly larger; trend initially indistinguishable because the synthetic decay is near-linear (it hides under the data polyline) — re-verified with a deliberately kinked rep 4-6 where the blue dashed trend clearly separates; chrome unregressed. |
| 13.5 | VBT set analysis: stop-set threshold config (default 20%, 5–40, persisted), headline cards (REPS / SET TIME / VEL. LOSS), "Stop set recommended" banner when loss ≥ threshold (which rep crossed), uncalibrated px/s warning chip with Calibrate link, velocity-loss % vs rep 1 computed in tracker-core (TDD) | M | done (sonnet-5, 2026-07-16) | tracker-core (`rep_metrics.rs`): `velocity_loss_percent(&[RepMetrics]) -> Vec<Option<f64>>` (design formula `loss_i = (1 - mean_i/mean_1)*100`, rep 1 → `None`, guards a zero/non-finite rep-1 mean by returning `None` for every rep rather than propagating NaN/inf); `stop_set_evaluation(&losses, threshold_pct) -> Option<StopSet{rep_index, loss}>` (first rep at/over threshold); `set_duration_seconds(&[RepMetrics]) -> Option<f64>` (rep 1's `start_t` to last rep's `end_t`, the "SET TIME" card) — all three TDD'd (9 new tests). tracker-app: `palette::LossSeverity`/`loss_severity`/`loss_severity_color` (dark mode uses the design's exact hex `#3fbf77`/`#d9a53f`/`#e05252`; light mode reuses `StatusKind::Success`/`Warn`/`Error`'s already-proven ≥3:1-contrast colors) — 4 new tests. `theme.rs` renamed its persisted file `theme.json` → `settings.json` (schema now `{dark_mode, stop_threshold_pct}`), silently falling back to reading the legacy `theme.json` (dark_mode only) when `settings.json` doesn't exist yet, so a 12.4-era install's theme override survives the rename with no explicit migration step; `theme::load_stop_threshold`/`save_stop_threshold` added alongside the existing `load_override`/`save_override`. `TrackingSettings` gained `stop_threshold_pct` (default 20 via new `DEFAULT_STOP_THRESHOLD_PCT`), restored from `settings.json` in `TrackerApp::load_video` (kept out of `AppState::new` itself so `state.rs`'s many direct-construction tests stay IO-free) and edited via a new DragValue (5–40%, saved on change) in the side panel's "Tracking settings" section. `SessionResults` gained `loss_percent: Vec<Option<f64>>` (parallel to `metrics`/`reps`, computed once in `build`) plus `set_duration_seconds()`/`stop_set_evaluation(threshold)` helpers, so 13.3's table and 13.4's chart can consume the same numbers rather than recomputing them. `results_section` (`side_panel.rs`) gained three headline cards (REPS / SET TIME / VEL. LOSS, the last loss-severity-colored via a new `headline_card` helper), a red-tinted "Stop set recommended" banner (exact design copy: "Velocity loss reached X% — over your N% threshold at rep R.") shown once any rep crosses the threshold, an amber uncalibrated chip ("Calibration not set — values shown in px/s. Calibrate for m/s.") when the unit isn't m/s, and a 5th "loss" column on the per-rep grid (color-coded, "—" for rep 1). fmt/clippy(`-D warnings`)/test clean workspace-wide (181 tracker-core + 207 tracker-app tests passing); smoke: no-arg and video-arg GUI launches both verified running. |
| 13.7 | Design-fidelity pass (user finding: GUI ≠ design): wire ChromePalette into egui global Style/Visuals via `palette::apply_chrome(ctx, dark_mode)` (panel/window/widget fills, hairline strokes, rounding, selection accent; dark = design hex, light = contrast-tested equivalents), `section_card` chrome around every side-panel section, right panel 460px on new `side_bg` (#18181b dark), hint bar as quiet strip (severity colors only for real warnings), buttons per design via global widget visuals, filmstrip collapsed by default once results exist (segments become primary nav; still shown pre-tracking). Acceptance: dark-mode Xvfb screenshot side-by-side vs the mock (scratch XDG_STATE_HOME for dark_mode, never the user's settings.json); light-mode contrast tests stay green. Full spec: docs/design/vbt-tracker-ui-notes.md "Design-fidelity gap audit" | M | done (worker, 2026-07-17) | Root cause fixed: `palette.rs` grew `chrome_visuals(dark_mode) -> egui::Visuals` (pure, TDD'd: panel_fill=app_bg, window_fill=panel_bg, widget fills #26262b / border #3a3a40 / hover #2e2e34, rounding 4.0, selection+hyperlink=accent) and `apply_chrome(ctx, dark_mode)` installing it. `ChromePalette` extended with `side_bg` #18181b, `toolbar_bg` #1d1d21, `button_bg` #26262b, `button_border` #3a3a40, `hint_bg` #202024, `hint_text` #9a9aa2 + light equivalents; new contrast tests (hint pair ≥3:1, default text ≥4.5:1 vs every new surface, visuals-mapping test). Integration with 12.4's theme logic: `toggle_theme` calls `apply_chrome`; `TrackerApp::update` computes effective dark = override.unwrap_or(current `visuals.dark_mode`) and re-applies only when `dark_mode` or `panel_fill` drifted from the palette — so system-theme-follow keeps working with no override, and stock `Visuals::dark()/light()` is gone everywhere. `side_panel.rs`: PANEL_WIDTH 320→460, panel frame fill `side_bg` + 14px margin, new `section_card` (panel_bg fill, border stroke, rounding 6, 14px margin) wrapping guide/status/settings/results/events; separators dropped; `headline_card` re-styled to the same chrome (was `Frame::group`). `banner.rs`: Working/Done/Neutral render as the design's quiet strip (hint_bg/hint_text); only ActionNeeded keeps `banner_colors` severity (banner contrast tests untouched). `toolbar.rs`: framed with `toolbar_bg` + hairline border. `thumbnail_panel.rs`: filmstrip collapsed by default once `results.is_some()` (segments = primary nav), open pre-tracking; small "🎞 Filmstrip ▸/▾" toggle button at the top of the strip panel persists the user's choice (`TrackerApp::filmstrip_override`); nothing deleted. Visual acceptance (fable-5 recipe: temp `preview_results.rs` 6-rep decaying-sinusoid harness on the real test video, scratch `XDG_STATE_HOME` settings.json for theme, Xvfb 1300x1600 + `import`; harness + temp `SessionResults` re-export reverted before commit): dark screenshot matches the mock's shell — #141416 app bg, #1d1d21 toolbar, quiet hint strip, 460px #18181b right panel with bordered cards, dark bordered buttons, filmstrip collapsed, chart/table/selection unregressed; light mode coherent (white cards on off-white, no white-on-white). Deliberate deviations from the mock: egui default fonts (no IBM Plex), guide/status/settings/events keep their existing content layout inside the new cards (mock shows a results-only panel), filmstrip toggle row remains visible at the bottom (mock has no filmstrip at all), video area letterboxes on plain app bg rather than the mock's hatched placeholder. 255 tracker-app tests green, fmt/clippy `-D warnings` clean. |
| 13.6 | Live mode panel per design: REPS card ("rep N in progress"), PHASE card (Eccentric/Bottom/Concentric, live from partial path), velocity-loss progress bar vs rep 1 with threshold marker, completed-reps mini list, live velocity readout overlay on video (big number + phase arrow) | M | todo | Extends 10.8's live rep counter |

## Milestone 14 — strategy shootout + strategy education (grilled 2026-07-16, fable-5)

User asks: (a) CLI runs all 6 strategies over the whole video at once and writes 6 overlay videos to eyeball which bar path wins; (b) GUI explains what each strategy/setting actually does (what is Template? what does "Test strategies" do? what do the custom knobs mean?) and links to the GitHub docs; (c) docs/theory.md grows a per-strategy deep-dive — ELI5 + engineer-level, written for a Rust developer, with collapsible snippets of the *real* code and the math.

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 14.1 | CLI strategy shootout: `compare` gains `--full` (whole video instead of the ~200-frame segment) and `--export-overlays` (per strategy, render `<stem>.<strategy-slug>.overlay.mp4` via the existing overlay renderer); table/JSON/recommendation unchanged; slugs filename-safe (e.g. `gaussian1.5-template`); GUI untouched | M | done (worker, 2026-07-17) | Overlays reuse `overlay_export::render_overlay_video` via a new pure `outcomes_to_bar_path` (seed = point 0, outcome i = frame seed+i+1, misses skipped so gaps show as breaks); rendered one at a time so memory stays flat. `--out-dir` added (default: next to source video) so smoke runs don't write into `test_videos/`. `--full` with ffprobe-unknown frame count falls back to decode-until-EOF (`u64::MAX` frames, capped prealloc). GUI `run_benchmark`/`spawn_benchmark` untouched; CLI loop unrolled to keep outcomes in hand for the render. Smoke (v1, seed 789 @ 312,430): 200-frame run + 6 overlays in 37s, each mp4 ~4.1MB / 31.35s per ffprobe, metrics identical to 11.4's `out/compare/v1.json`; `--full` run (1120 frames/strategy) 1m57s, winner unchanged (`gaussian:1.5/color`, template caveat still applies); full-video overlay smoke skipped for time budget |
| 14.2 | GUI strategy education: hover tooltips + a "What do these do?" collapsible in Tracking settings explaining Template (ZNCC patch matching), Color (color-model region tracking), the auto-suggestion, gaussian/median filters, "Test strategies" (what the benchmark measures, what Apply winner writes), and each advanced knob; plus a "Docs" link button opening the GitHub repo page (docs/theory.md) via the existing open-folder spawn pattern (xdg-open/open URL) | M | done (worker, 2026-07-17) | Settings section extracted to new `app/settings_section.rs` (side_panel.rs was 1024 lines pre-task; the education copy would have pushed it well past ~1100 — moved `tracking_settings_section`/`strategy_benchmark_section`/`advanced_tuning_row(_f64)` verbatim, side_panel.rs now 760 lines and back to guide/status/results/events only). All user-facing copy lives in `settings_section::education` as named consts: 18 tooltips (tracker combo + each of Auto/Template/Color inside it + the Auto suggestion line, both filter checkboxes + σ + k, stop-set threshold, Test strategies + Apply winner, all 6 advanced knobs) + 4 `EXPLAIN_*` paragraphs + `DOCS_URL`. Tooltip semantics taken from `tracking.rs`'s `TrackerTuning`/session-config docs (min score = below is a Miss/gap opens; coast limit = interpolated frames before pause-for-reseed; reacquire min score = stricter mid-gap gate against background false-locks; update threshold = adaptive-template refresh gate) and `suggest.rs`'s heuristic (saturation + annulus color-rarity) — not guessed. "What do these do?" is a nested CollapsingHeader (default collapsed) with 1–3-sentence ELI5s condensed from theory.md §7.1–7.5, each citing its § for depth. Docs link: `ui.hyperlink_to` chosen over the `open_containing_folder` spawn pattern after verifying eframe 0.29 enables egui-winit's `links` feature (webbrowser crate opens the URL natively, non-blocking, with its own failure logging) — the manual spawn was not also shipped. TDD: 4 tests on the copy consts (all 22 substantial prose + no doubled spaces from string-continuation slips, DOCS_URL shape ends `#7-strategy-deep-dive`, explainers cite §7.1/7.2/7.3/7.5, advanced tooltips contain the load-bearing terms "Miss"/"reseed"/"interpolated"/"mid-gap"/"refresh"/"200 frames"). 241 tracker-app + 181 core tests, fmt/clippy `-D warnings` clean. Visual check (Xvfb + import): default 800×600 window scrolls Tracking settings below the Status fold and no xdotool/xte on the box, so per 13.3's sanctioned pattern a temporary *uncommitted* tweak (window 1000×980 + the two headers `default_open(true)`, both reverted before commit, gates re-run after revert) produced a real open-state screenshot: combo/suggestion/filters/threshold/Advanced/Test strategies/"What do these do?" all render, Template ELI5 paragraph wraps cleanly, chrome (toolbar/guide/status/thumbnails/status bar) unregressed. Hover tooltips themselves can't be screenshotted headlessly (no pointer injection) — verified by code + the no-doubled-space test. |
| 14.3 | docs/theory.md per-strategy deep-dive: for each of Template/ZNCC, Color model, GaussianBlur, Median — an ELI5 paragraph ("explain like I'm 5") AND an engineer section (algorithm walkthrough, the actual math with formulas, complexity), written Rust-first: what crates-free constructs the impl uses, `<details>`-collapsed snippets of the real tracker-core code with line references, worked numeric mini-examples; cross-link from README and from 14.2's GUI copy | M | done (worker, 2026-07-17) | New theory.md §7 "Strategy deep-dive" (7.1 Template/ZNCC, 7.2 Color model, 7.3 GaussianBlur, 7.4 Median, 7.5 "Test strategies" benchmark), inserted *before* the experiment log, which was renumbered §7→§8 (2 in-doc references updated) so the log stays the closing section per the doc's own framing. Each strategy: quotable one-line ELI5 (written for 14.2 tooltip reuse), engineer section (ZNCC sum + max(anchor,adaptive); HSV 3-band check + centroid + fill-fraction score; separable kernel w(x)=exp(−x²/2σ²), radius=ceil(3σ); median-of-k² with clamp-to-edge), complexity (O(R²·r²) template ≈4.7M madds at defaults vs O(R²) color; separability 22 vs 121 taps at σ=1.5), robust/fragile linking §2's affine proof + §5's filter theory rather than repeating them, Rust walkthrough with verbatim `<details>` snippets at metric.rs:25, tracker.rs:217, color.rs:148/206, color_tracker.rs:271, preprocessor.rs:137/162/193 (all re-verified byte-exact against source pre-commit), enum-vs-dyn rationale quoted from preprocessor.rs:24 module docs, and worked numerics (3×3 ZNCC: affine=1.0 exactly, one blown pixel→0.61 — happens to land between min_score and update_threshold, nice §3 illustration; σ=1.5 → 11-tap kernel table, impulse ×0.266²≈+7.1 spread-not-removed; median k=3 kills a 255 impulse exactly, 8-to-1 outvote). §7.5 defines tracked%/misses/gaps/reseeds/mean-score/jitter and `recommend`'s tie-break chain (tracked% → jitter, None=∞ → matrix order) without a compare.rs snippet — 14.1 is concurrently editing that file, so prose over line-pinned quotes there. README architecture note now deep-links `#7-strategy-deep-dive`. No code changes. |

## Milestone 15 — third user session findings (grilled 2026-07-17, fable-5)

User session on v4 (22.56.32.mp4, 60fps): 3 phantom reps counted during walkout/unrack before any squat; wants a path-overlay toggle while tracking; wants the seed to be a visible/resizable region.

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 15.1 | BUG: phantom reps during walkout/unrack — `RepSegmentationConfig` gains `min_displacement` (min vertical ROM per rep; default ~40px uncal / ~0.15m cal via `rep_segmentation_config`), candidate reps below it discarded in `segment_reps` (TDD: walkout-like small oscillation → 0 reps, real-amplitude sinusoid unchanged); live counter + SessionResults both benefit (shared config). Also fix banner vs status-bar frame-count off-by-one ("541/3778" vs "541/3777") | M | done (worker, 2026-07-17) | `min_displacement` gate is **per-phase** (eccentric AND concentric must each cover it, via trapezoidal integration of `vy` over the run), not total ROM: rejects asymmetric drift (long slow descent + tiny bounce) that total-ROM would pass, and for a real rep the two agree anyway (idle bottom pause integrates to ~0). Undersized candidates are discarded outright (both phases consumed), never merged — new test proves a walkout wobble *between* two real reps still yields exactly 2. Default 40.0 px (real squat ROM ~180 px in our footage, walkout wobble ≪50 px); calibrated override 0.15 m in `rep_segmentation_config` — CLI, SessionResults, and live counter (10.8) all flow through that one fn (verified all 3 call sites). Boundary: `>= min_displacement` kept, strictly below discarded (deterministic test: constant 30 px/s over exactly 1 s integrates to exactly 30.0). TDD red-first for the walkout series, threshold boundary, calibrated m-unit variant. Existing tests' `clean_rep` ramped vy to only 50 → ~25 px travel (no real rep is that small) — bumped to 400 (~190 px) in `clean_rep` + `pause_at_bottom` with comments; all other segment_reps tests unchanged. Off-by-one: banner's `Phase::TrackingPath.total` used raw `metadata.frame_count` while the bottom bar shows `frame_count-1` (last valid 0-based index, same as the seek slider max) — banner now also `saturating_sub(1)`; two state tests updated to pin the convention. Sanity CLI rerun on v4 with the documented seed (frame 300, 300,150): identical output pre/post change (both "no reps detected" for that headless seed — the phantom-3 came from the user's GUI live-counter session with a different seed), i.e. no regression from the gate on real data. fmt/clippy -D warnings/workspace tests green; no GUI visual check (pure logic + string fix). |
| 15.2 | Path overlay toggle: checkbox/eye button (video panel or settings) to show/hide the live path polyline + marker during tracking and in review; persisted in settings.json | S | done (worker, 2026-07-17) | Placement: "👁 Path" `toggle_value` in the scrub bar's transport row (after next >>) — same row live and in review, so one discoverable spot covers both; tooltip "show/hide the bar path overlay on the video". Scope decision: `show_path` gates the Review polyline (`draw_bar_path`) *and* its white current-position crosshair, but the live tracking crosshair (green/gray lock-on) and the red Seed marker stay visible regardless — hiding lock-on feedback would make a hidden-path run look broken. (There is no live polyline today — the only path polyline is results-driven — so live the toggle only affects post-Done overlay.) `AppState.show_path: bool` default true (2 state tests); settings.json schema gains optional `show_path` via `theme::load_show_path`/`save_show_path` (missing field → None → default shown, legacy theme.json fallback sets None; round-trip + missing-field tests); restored in `TrackerApp::load_video` next to `stop_threshold_pct`, save-on-change from the toggle. Xvfb verification (dark scratch XDG_STATE_HOME, temp preview_results 6-rep sinusoid harness + temp SessionResults re-export, both reverted): path-on shows polyline + white marker + pressed accent toggle; path-off both gone, video/chrome unregressed, toggle unpressed. fmt/clippy -D warnings/tests green (262 tracker-app). |
| 15.3 | Seed region visibility + sizing: draw the template patch rectangle around the seed on the video (placing + placed states); drag corner (or scroll/DragValue) resizes = sets `patch_radius`; patch radius already an Advanced knob — surface it visually; region preview updates live | M | done (worker, 2026-07-17) | Region semantics v1: seed point + `patch_radius` IS the region (center + extent, side `2r+1` source px — exactly the square `extract_patch` cuts); non-square/offset free rectangles stay future work. Single source of truth: new pure `effective_patch_radius(&TrackingSettings)` in `state.rs` (settings value clamped into new shared `PATCH_RADIUS_RANGE: 4..=64`; TDD — default 12, override, clamp both ends, range-covers-default) — the drawn rect, the scroll gesture, and the Advanced DragValue (`settings_section.rs` now uses the shared const instead of a literal `4..=64`) all agree. `video_panel.rs`: `draw_patch_region` maps the square through the existing letterbox/scale mapping (`image_px_to_screen` center + per-axis scale × `r+0.5` half-extent, so it tracks zoom/fit exactly like the crosshair); two-tone stroke (1px black-alpha outline outside 1px chrome-accent) for visibility on any content. Drawn in three places: hover preview under the cursor while `Mode::PlacingSeed` (region visible *before* clicking), around the placed seed on its frame, plus `draw_patch_hint` label "patch N×N px — scroll to resize" on a translucent backdrop (placing mode only). Resize: `raw_scroll_delta.y` over the hovered video while PlacingSeed steps `settings.patch_radius` ±1/event, clamped to `PATCH_RADIUS_RANGE` — writes the same field the DragValue edits, no second storage. Xvfb visual check with the real 14.03.30 video (temp env-var harness in `mod.rs` `update` injecting seed 300,150 + optional radius, reverted; note: must `env -u WAYLAND_DISPLAY` or the app connects to the session's Wayland compositor and the Xvfb screenshot is blank): default r12 → crosshair + "patch 25×25 px" hint (rect ≈9 screen px at the 0.36 fit scale, sits under the crosshair); harness r40 → clearly visible accent square + "patch 81×81 px" hint, proving the rect tracks the knob and scales through the screen map; chrome unregressed. fmt/clippy -D warnings/workspace tests green (268 tracker-app lib). |
| 15.4 | BUG: Stop ignored while paused-awaiting-reseed — worker blocks on `reseed_rx.recv()` (NeedsReseed) and never reads `control_rx`, so ControlCommand::Stop queues unread (user hit Stop twice, run kept going). Fix: reseed-wait must also observe control channel (unify channels or poll both). Semantics UX: rename Stop→"Finish" with tooltip "end the run now, keep results so far — not resumable; use Re-track to start over"; after finish, Review state must make Re-track/New session obvious | M | done (worker, 2026-07-17) | Found in third user session (stop requested ×2 in events, state still tracking). Fix shape: `recv_timeout(50ms)` alternation in `run_tracking_loop`'s reseed wait (std mpsc has no select; crossbeam not a dep, not added; unifying the channels would have touched every sender for no behavioral gain) — on each timeout `control_rx` is drained via `try_recv`, Stop returns `Ok(())` into the existing Done-with-partial-samples path; Pause/Resume during a reseed wait are documented no-ops (frames already aren't being consumed). 10.1's channel-closed semantics preserved verbatim: `Disconnected` still ends the run cleanly (regression test `run_tracking_loop_ends_cleanly_when_paused_awaiting_reseed_and_channel_closes` stayed green untouched). New TDD test `stop_command_is_honored_while_paused_awaiting_reseed` (red first: hung >60s on the old code; keeps `reseed_tx` alive so Stop, not channel-close, is what ends the wait). UX: toolbar button Stop→"Finish" with the not-resumable/Re-track tooltip, extracted `finish_discard_buttons` now also rendered in the reseed-pause branch (previously only yellow label + Resume — exactly where the user got stuck); event message → "finish requested: ending the run with results so far". Review's Re-track/New session verified already reachable after Finish (worker sends normal Done → `poll_tracking` → Review; no change needed). Xvfb light screenshot: chrome unregressed; Finish itself needs an active run (seed click) which can't be staged headlessly — button shares the exact `stop_tracking` path the tests cover. |
| 15.5 | Live path polyline: draw the in-progress bar path during tracking (buffer Progress positions on TrackingRunState, polyline gated by 15.2's show_path; keep cheap — no per-frame allocation), so the user watches the path grow instead of waiting for Done | S | todo | Found during 15.2: only a results-driven polyline exists today |

## Milestone 11 — image-processing theory & pluggable strategies (grilled 2026-07-16)

Decisions: contributor-facing [`docs/theory.md`](docs/theory.md) with academic citations (user's ask: document noise reduction, contrast, seed pinpointing theory). Key existing fact to document: ZNCC is already brightness/contrast-invariant by construction — histogram equalization/CLAHE deliberately not needed (→ ROADMAP if ever). Filters run region-level (patch + search window, ~100× cheaper than whole frame), identically on reference and candidates (see CONTEXT.md "Preprocessor"). Manual switching + short-segment auto-benchmark; full parameter sweep → ROADMAP.

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 11.1 | `docs/theory.md`: pipeline stages (decode → grayscale → template/color matching → gap logic → smoothing → velocity → reps), why ZNCC + contrast-invariance math, dual-template rationale + drift theory, noise sources (sensor grain, WhatsApp compression, motion blur) and filter theory (Gaussian vs median), why-not CLAHE/sharpening, citations w/ links (Lewis 1995 fast NCC; Pan et al. 2009 DIC review; Brown 1992 registration survey; standard filter references) | M | done (sonnet-5, 2026-07-16) | 7 sections + experiment log seeded from e2e-results.md (rotation bug, 3.6 dual-template, 10.2b sweep); 4 citations web-verified (Lewis 1995, Pan et al. 2009 doi:10.1088/0957-0233/20/6/062001, Brown 1992 doi:10.1145/146370.146374, Matthews/Ishikawa/Baker 2004 doi:10.1109/TPAMI.2004.16); linked from README architecture section |
| 11.2 | `Preprocessor` port in tracker-core (pure, dependency-free): chainable region filters — GaussianBlur(σ), Median(k), identity default; TDD (impulse/noise attenuation, edge behavior, chain order); applied to both Template patches and Color regions, identically to reference + candidates | M | done (sonnet-5, 2026-07-16) | New `preprocessor.rs`: closed enum `Preprocessor { Identity, GaussianBlur { sigma }, Median { k } }` + `PreprocessorChain` (ordered `Vec<Preprocessor>`), operating on a `width x height` row-major `f32` plane — a deliberate choice over a `dyn` trait object, documented in the module docs: the filter set is small/fixed (CONTEXT.md names exactly these two), and a concrete `Clone + PartialEq + Debug` type is what lets `TemplateTrackerConfig`/`ColorTrackerConfig` embed an optional chain (dropping their `Copy` derive, confirmed safe — no call site relied on implicit-copy reuse) without `Box<dyn ..>` plumbing. Gaussian: separable two-pass convolution, kernel radius `ceil(3*sigma)` (≥1), clamp-to-edge boundary handling (no zero-padding darkening). Median: `k x k` neighborhood (even `k` truncates to `k-1` via integer radius `k/2`), same clamp-to-edge handling. `TemplateTracker` filters via `PreprocessorChain::apply_patch` (a new `Patch::from_values` public constructor rebuilds a `Patch` from a filtered plane) — applied to the anchor/adaptive template at construction and to every candidate patch in `step`, honoring CONTEXT.md's Preprocessor same-space invariant (also cross-referenced into docs/theory.md §5, which this task's design follows verbatim: "region-level filtering ... after `extract_patch`, before `Zncc::score`"). `ColorTracker` has no patch-shaped region to reuse, so it filters an internal `RgbRegion` (three independent `f32` planes, R/G/B) by calling the *same* `apply_plane` three times — documented as exactly the same per-plane computation, since neither filter has cross-channel coupling; new `ColorTracker::learn_and_new(frame, seed, radius, model_config, tracker_config)` filters the seed's RGB patch before learning the `ColorModel` (so the reference is learned in the same filtered space `step` scans candidates in), while the existing `ColorTracker::new(model, config)` remains for callers with an already-learned model and documents that bypassing `learn_and_new` skips that guarantee. TDD'd: Gaussian preserves flat regions within 1e-4 and cuts impulse-noise variance >50%; median fully removes a single salt/pepper pixel and preserves a hard step edge exactly; both clamp-to-edge (no border darkening/degenerate 1x1 panic); chain order changes the result (median-then-gaussian vs. gaussian-then-median measurably differ, median-first suppresses an impulse more) and an empty chain is a byte-exact no-op; `TemplateTracker`/`ColorTracker` each get a filtered-tracker self-match-≈1.0 test on their own seed frame, plus a synthetic moving-noisy-square sequence proving a Gaussian-chain `TemplateTracker`'s cumulative score is never worse than an unfiltered one (honestly constructed via a reproducible LCG impulse-noise generator, not cherry-picked). fmt/clippy(`-D warnings`)/test clean workspace-wide (173 tracker-core + 157 tracker-app tests, up from 167/157). |
| 11.3 | Wire strategies: CLI repeatable `--filter gaussian:1.5` / `--filter median:3`; GUI side-panel "Tracking settings" (collapsible): filter chain picker + tracker kind + thresholds, editable before Track/Re-track | M | done (sonnet-5, 2026-07-16) | CLI: repeatable `--filter gaussian:<sigma>`/`--filter median:<k>` on `track`, parsed by shared `parse_filter_spec` (`cli.rs`) into an ordered `PreprocessorChain` on `TrackerTuning` (now `Clone`-only, no longer `Copy` — it owns a `Vec<Preprocessor>`); flag order == chain order (TDD: valid/invalid/multiple/order-preserved/no-flag-stays-empty). New `tracking::color_tracker_config(TrackerTuning)` (mirroring `tracker_config`) so the chain reaches the `ColorTracker` path too, not just Template. GUI: new `AppState::settings: TrackingSettings` (tracker kind, gaussian/median enable+params, advanced tuning knobs) read fresh by `start_tracking` every run — no separate "apply" step, so editing between runs takes effect on Re-track by construction; side panel gained a "Tracking settings" `CollapsingHeader` (tracker-kind `ComboBox` showing the live `suggested_tracker`, filter checkboxes+DragValue/ComboBox, advanced knobs behind a nested `CollapsingHeader`), wrapped in `add_enabled_ui(!running, ..)` so it visibly locks while a run is active rather than being hidden; `side_panel::show` changed from `Option<&AppState>` to `Option<&mut AppState>` to allow this. `start_tracking`'s "track started" event now reads `"tracking started: <describe()> @ frame N"` (e.g. "tracking started: template, gaussian σ1.5 + median 3"), `describe()` new on `TrackingSettings`. Filter chain order fixed gaussian-then-median for v1 (documented in `TrackingSettings`'s doc comment and in the panel's UI copy) — a reorder UI is future work. fmt/clippy(`-D warnings`)/test clean workspace-wide (173 tracker-core + 168+11 tracker-app tests); smoke: no-arg GUI launch and `track --filter median:3 --filter gaussian:1.5` CLI run both verified. |
| 11.4 | Strategy benchmark: `tracker-app compare <video> --seed-frame N --seed X,Y` — ~200-frame segment × strategy matrix (none/gaussian/median × template/color), table: mean score, misses, gaps, jitter; recommends winner. GUI "Test strategies" button (background thread, results in panel) | M | done (sonnet-5, 2026-07-16) | New `compare.rs`: fixed 6-entry `strategy_matrix()` ({none,gaussian:1.5,median:3} × {template,color}, forced kind, never `Auto`), pure `compute_metrics` (tracked%, misses, streak-based gaps, coast-limit-triggered auto-reseeds, mean score, mean jitter = mean Euclidean distance between consecutive *tracked* samples) and pure `recommend` (highest tracked%, tie-break lowest jitter, `None`-jitter loses every tie, earliest matrix entry wins a full tie) — both TDD'd directly with synthetic outcome vectors, no video/ffmpeg needed. `run_strategy`/`run_benchmark` re-decode the segment once per strategy (a fresh `FfmpegFrameSource` per strategy rather than one shared in-memory buffer — the 11.2/11.3 same-space-filtering invariant makes a shared buffer a correctness footgun, and 6 short re-decodes of ~200 frames is cheap); color strategies where `suggest_tracker` would itself recommend Template get an advisory note but still run. CLI `compare <video> --seed-frame N --seed X,Y [--frames 200] [--out path.json]` prints an aligned table + recommendation line and optionally writes machine-readable JSON. GUI: `AppState` gained `benchmark`/`benchmark_progress`/`benchmark_rows`, `start_strategy_benchmark`/`poll_benchmark`/`apply_benchmark_winner` (mirroring the `TrackingHandle`/`poll_tracking` shape), a "Test strategies" button in the side panel's "Tracking settings" section (enabled via `can_test_strategies`: seed placed, no tracking or benchmark already running), a compact strategy/tracked%/jitter results grid with the winner highlighted, and "Apply winner" writing the winning filter+tracker into `state.settings`. Real runs (`out/compare/v1.json`, `out/compare/v3.json`) and their interpretation are in docs/theory.md §7 (2026-07-16 entry) — headline: v1's winner carries a color-indistinct-seed caveat worth reading past the raw numbers, v3 needs no filtering at all. fmt/clippy(`-D warnings`)/test clean workspace-wide (173 tracker-core + 188 tracker-app tests); smoke: no-arg and video-arg GUI launches both verified running (timeout-killed, not crashed). Full auto-tune sweep → ROADMAP. |

## Milestone 16 — smoke matrix, results dashboard & VBT grounding (2026-07-21)

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 16.1 | `docs/theory.md` §9 "The sports science behind the numbers (VBT)": load–velocity relationship, MVT, velocity loss; MV vs MPV vs PV (and why we deliberately don't compute MPV — §6's noise-amplification argument); VL-threshold evidence backing 13.5's 20% default; camera-vs-LPT validity table with our known error sources; cross-refs to `rep_metrics.rs`/`rep.rs`/§6 | S | done (fable-5, 2026-07-21) | 9 web-verified citations (Weakley 2021 S&C J; Jukic 2023 Sports Med meta-analysis; García-Ramos 2018 JSCR; Andersen 2021; Held 2020; Ruf 2018 deadlift MVT caveat; Pérez-Castilla 2020 PLOS ONE; smartphone-app concurrent validity PLOS ONE 2024; Balsalobre-Fernández 2017). Key design consequence recorded: 20% VL is a defensible default, not a truth — stop-set banner must stay a coaching cue and the threshold user-configurable |
| 16.2 | Extend `scripts/smoke-report.sh` to the full 4-video × 6-strategy matrix: drive `compare --full --export-overlays` per test video (seeds from docs/e2e-results.md), assert per-video that at least one strategy tracks ≥95% with no wild jumps, and emit a matrix table (video × strategy: tracked%, gaps, reseeds, jitter, reps found) plus overlay paths into `docs/smoke/YYYY-MM-DD.md` for human visual review. Keep the existing single-video quick checks as a fast mode (`--quick`) so CI stays cheap | M | todo | The 4×6 full-length run takes ~10–20 min wall clock and writes 24 overlay MP4s; full mode is a pre-release/manual gate, not a per-PR CI job |
| 16.3 | Results dashboard from exports (GUI): load an existing `<video>.csv` / `.json` / `.reps.json` without re-tracking ("Open results…"), and render the path/velocity/rep views from it — makes every past run re-inspectable and decouples charting from the tracking worker | M | todo | Prerequisite for session history (ROADMAP). Reuses 13.3/13.4 widgets; the export files become the app's de-facto session format, so their schema is now a compatibility surface — version it |
| 16.4 | GUI graphs beyond 13.4: velocity-vs-time trace for the whole set (with rep phase bands), 2D bar-path plot (x vs y, aspect-correct), and per-rep depth/peak/mean bars; all reading the same in-memory series the dashboard loads, all exportable as PNG | M | todo | 12.7's deferred "VBT graphs" finally lands here; label every axis with its unit (px or m — `Calibration` presence decides) so an uncalibrated run can never be misread as metric |
| 16.5 | Live dashboard during tracking: stream frame events (position, phase, provisional rep count, live velocity) into the dashboard views as the worker runs, rather than only at Review | M | todo | Depends on 13.6 + 15.5 (live panel + live polyline) landing first — same Progress event stream, so do those before this |
| 16.6 | BUG (user finding 2026-07-21, v3 overlay): off-axis seeding on the plate face makes the tracked point orbit as the plate spins in the sleeve. Measured on v3 (seed frame 300): seed `260,120` sits r≈35px from the plate axis → x stdev 69.1px, mean\|dx\| 0.50; on-axis seed `284,94` → x stdev 30.1px, mean\|dx\| 0.36, vertical range unchanged (497 vs 499px). Plate ≈345px/450mm, so r=35px ≈ 45mm of pure rotation artifact injected into every sample. Re-pick all four `docs/e2e-results.md` seeds on-axis (v1 `325,271`, v2 `285,425`, v3 `284,94`, v4 `230,146`) and record the method: chrome sleeve ring where concentric (v1/v2), plate-circle center otherwise (v3/v4 — their bright end cap is itself ~29px off-axis) | S | in-progress (fable-5, 2026-07-21) | The tracker was never wrong here — it faithfully followed a point that really was moving in a circle. Pure-numeric review could not have caught this: tracked% was 100 and jitter *low*, because an orbit is smooth. Third instance of the recurring lesson in the review log (3.4/3.5 rotation, 10.9 duplicate frames) that a clean metric ≠ a correct one |
| 16.7 | Rotation-invariant position by construction: fit the plate circle per frame (Hough-style circle fit or radial-gradient center estimation over the search window, seeded by the user's click) and report **the fitted center** as the Bar Path position instead of a template patch's location. Makes the metric independent of where on the plate the user clicked, and independent of plate spin — removing the 16.6 error class rather than documenting around it. New `Tracker` impl in tracker-core (pure pixel math, dependency-free), selectable alongside Template/Color; `suggest_tracker` should prefer it when a strong circular edge is present near the seed | M | todo | User-chosen over "warn in GUI" and "docs-only guidance" (2026-07-21). Design notes: the plate rim is the strongest circular edge in all four test clips; fitting the *rim* rather than the hub also gives a per-frame pixel→meter scale for free (known plate diameter = 0.450m competition), which could later replace or cross-check manual `Calibration`. Radius should be near-constant across frames — a sudden radius change is a strong lost/occluded signal, better than a correlation score. Add to the `compare` strategy matrix as a 7th strategy so it is benchmarked against Template/Color on the same footage |
| 16.8 | Update `docs/theory.md` §9.4's camera error table + a new §7.6 with the plate-spin error class and the circle-fit rationale from 16.7; add seeding guidance ("seed the plate axis, not the plate face") to the in-app guide banners (10.7) | S | todo | §9.4 currently lists perspective, lens distortion, handheld motion, frame rate and calibration scale as error sources — plate spin is a sixth, and unlike the others it is introduced by the *user's click*, not the camera |

## Milestone 17 — tracking correctness (audit 2026-07-21)

Full audit: [docs/design/tracking-audit-2026-07-21.md](docs/design/tracking-audit-2026-07-21.md).
Trigger: user visual review found the tracker drifts off the bar onto rack
hardware while reporting 100% tracked / 0 gaps / ZNCC 0.996. Supersedes 16.7
(folded into 17.5). Deliberately contains no threshold-tuning tasks — six
existing knobs were each added to fix an earlier instance of this bug class
and the failure survived all of them.

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 17.1 | Ground-truth fixtures + accuracy metric: hand-label true bar-centre on ~20 sampled frames per test video (80 total) as CSV beside the videos; test harness reports mean/max Euclidean error vs ground truth in px and plate-diameters; new column in `compare`, threshold assertion in CI | M | done (fable-5, 2026-07-21) | **Do first.** Audit F7: all five existing metrics (`tracked_pct`, gaps, reseeds, `mean_score`, `mean_jitter_px`) measure the tracker's *self-assessment*, so a confident false lock maximises every one of them — a stationary lock on rack steel has lower jitter and higher correlation than correct tracking of a moving bar. Project currently has no measurement of correctness, only of confidence; until it does every fix below is unfalsifiable  **Result: the metric works and the old ones were lying.** `gaussian:1.5/template` on v3 — which `compare` scored 100% tracked / 0 gaps / 0 reseeds / ZNCC 0.996 — puts only **15% of labelled frames within 0.1 plate-diameters of the bar**, mean error 0.60 plate-dia (80px). Unfiltered template is worse: 15% within tolerance, mean 1.33 plate-dia. Error grows monotonically from the seed (0→3→32→34→59→95px), i.e. drift, not noise. Both report a confident position on the `occluded` frame (false confidence = 1). Ground truth is user-labelled via `groundtruth/label.html` (browser tool + `serve.py`, built after tkinter proved unavailable); user's labels agreed with fable-5's to 1-3px on the 10 frames both did, after the target definition was corrected from plate-disc-centre to **bar sleeve end** (user finding: the disc centre is not the bar — parallax + plate sag). v1/v2 turned out to be ~2/3 `occluded` at the sleeve end on that rig, so they are mostly false-confidence tests.|
| 17.2 | `Track` state + motion model: replace `Tracker::step`'s `last_pos: Point` with state carrying position/velocity/uncertainty + `dt`; centre the search window on the *predicted* position; reject observations implying impossible acceleration for a loaded barbell; coast by predicting through occlusion rather than freezing | M | done (sonnet-5, 2026-07-22) | Audit F1/F2. Root finding: `velocity.rs` already derives exactly the state a motion model needs, but strictly downstream of tracking and never fed back — the system knows after the fact that the bar was moving 0.4 m/s down, and separately lets the tracker teleport it sideways onto a rack upright. Also replaces the mid-gap-*only* distance guard: today `max_reacquire_distance`/`reacquire_min_score` are gated on `open_gap_start.is_some()`, so silent drift (which never opens a gap) is entirely unguarded. Constant-velocity predictor + gating radius, not a full Kalman  **Impl:** new `tracker-core/src/motion.rs`: `Track { position, velocity: px/s, uncertainty: px }` with `predicted(dt)` (constant-velocity), `observed(pos, dt)` (re-derives velocity, resets uncertainty to 0 on a real Found), `coasted(dt, growth_per_second)` (predicts forward, grows uncertainty — this is the "coast through occlusion" fix), and `gate_radius(track, max_acceleration, dt) = 0.5*max_acceleration*dt² + track.uncertainty` (`s=0.5at²` bound, not a Kalman covariance). `Tracker::step` signature is now `fn step(&mut self, frame: &Frame, track: &Track, dt: f64) -> StepOutcome`; both `TemplateTracker` and `ColorTracker` centre their search window on `track.predicted(dt)` and reject a winning candidate outside `gate_radius` as `Miss` — this lives *inside* `step` itself, so it fires on every call, gap open or not (fixes F2: a same-frame teleport is now rejected even with `open_gap_start` unset, unlike the old `session.rs` guards). New config knobs: `max_acceleration` (px/s², default 6000.0) on both tracker configs; `TrackingSessionConfig::coast_uncertainty_growth` (px/s, default 20.0). `TrackingSession` gained a `track: Track` field (distinct from `last_pos`, which still anchors gap interpolation/`max_reacquire_distance` and only moves on a real Found) and a `track()` accessor; `step` takes `dt: f64`; `reseed` resets to `Track::new(point)` (no velocity history to trust, like the initial seed). dt is threaded from `Timebase::new(fps_num, fps_den).map(\|tb\| 1.0/tb.fps())`, falling back to 1/30s on degenerate fps metadata (same policy the overlay-only timebase already used) — computed once in `tracking.rs`'s `run_tracking_worker` and `compare.rs`'s benchmark entry points, passed down through `run_tracking_loop`/`run_strategy`/`run_benchmark`/`spawn_benchmark`. Call sites touched: `tracker.rs`, `color_tracker.rs`, `session.rs` (core); `tracking.rs` (`AnyTracker::step`, `run_tracking_worker`, `run_tracking_loop`), `compare.rs` (`run_strategy`'s own `Track` bookkeeping, `run_benchmark`, `spawn_benchmark`), `app/state.rs` (`start_strategy_benchmark`). Old `session.rs` mid-gap-only guards (`max_reacquire_distance`, `reacquire_min_score`) were left in place rather than deleted — now redundant for the acceleration case but still independently useful (score-based reacquisition strictness) and untouched by this change; all their tests still pass. Existing call sites across ~15 unit tests mechanically wrapped `Point` args as `&Track::new(point)` with `dt=1.0` (velocity 0 ⇒ prediction unchanged; generous default gate radius ⇒ unaffected). New TDD tests: `step_rejects_observation_beyond_gate_radius_even_without_a_gap` (the core F2 case), `step_accepts_observation_within_gate_radius`, `step_centers_search_on_predicted_position_not_last_observation`, `coast_predicts_forward_through_a_miss_instead_of_freezing`. tracker-core: 217 tests (was 208 pre-17.2, +9). Full workspace: `cargo test --workspace`/`clippy -D warnings`/`fmt --check` all clean. **Hotfix 2026-07-22 (user report, false loss at frame 25 on a clearly-visible bar, reseeding every few frames):** the accel-from-rest gate was fundamentally miscalibrated — `0.5*6000*dt²` = **0.83px at 60fps**, and since velocity is only learned *after* an observation is accepted, a fresh seed's stationary prediction rejected the object's very first real motion and never bootstrapped. Replaced with a **velocity reachability** gate: `gate_radius = max_velocity*dt + uncertainty`, measured from the last position (`track.position`), not the prediction. Config knob renamed `max_acceleration` (px/s²) → `max_velocity` (px/s), default 6000 → **3000** (≈50px/frame @60fps, ≈100 @30fps). This gate now only rejects gross teleports; slow drift is the 17.3 anchor veto's job (which was always the real drift fix, 15%→85%). New regression test `fast_motion_from_a_fresh_seed_is_tracked_not_lost`; `gate_radius` test + doc updated. tracker-core 222 tests. |
| 17.3 | Anchor as veto: `max(anchor_score, adaptive_score)` → accept only if the anchor also clears a floor, and refresh the adaptive only when the anchor agrees | S | done (fable-5, 2026-07-21) | Audit F3. `tracker.rs:227` — under `max`, a candidate the anchor *rejects* wins if the adaptive accepts it, so the anchor can never veto; and refresh is unconditional on anchor agreement, writing each small error back into the reference. Ratchet with no restoring force — sufficient on its own to walk off the object in ~10s. 3.6's design was right; the combinator inverts it. Testable with the existing s **Result on v3 (user labels): 15% → 85% within 0.1 plate-dia, mean error 178→25px (unfiltered), 80→35px (gaussian).** Through the actual lift (f300–f2500) every frame is now within tolerance, max 12px. Residual failures are all *after* the set: f2700 occluded + f2900/f3100 racked at frame edge still get a confident position — the no-Lost-state problem (F5), 17.4's job. Confirms F3's ratchet was the dominant cause of drift. Impl: `anchor_floor` config (default 0.3, below min_score); a candidate is eligible only if the never-changing anchor still scores it ≥ floor; among eligible, winner is still max(anchor,adaptive) so the adaptive refines but can't override identity. Rewrote the 3.6 `dual_template_stays_found…` test: it drove appearance to an anti-correlated pattern (anchor -0.19) while stationary — exactly the ratchet — now split into adaptive-absorbs-partial-change (anchor stays ≥floor) + veto-engages-past-floor.ynthetic-blend tests |
| 17.4 | Confidence as a first-class, tracker-independent concept: normalised confidence + tracker-specific raw score on `StepOutcome`; explicit `Lost` distinct from transient `Miss`; matching `Source::Suspect`/confidence column in exports so metrics can flag them; stop headless auto-resume-from-last-position when the cause was drift | M | done (fable-5, 2026-07-21) | Audit F4/F5. Vocabulary today is Found\|Miss, Tracking\|NeedsReseed, Tracked\|Interpolated — nothing expresses "tracked but probably wrong", so a false lock exports as `Tracked` and flows into velocity→reps as fact. `compare.rs:8` already documents that ZNCC correlation and colour fill-fraction are incomparable units, yet `session.rs` applies one `reacquire_min_score` scalar to whichever tracker is installed | **Diagnosis (2026-07-21), not yet fixed.** The honest confidence is the *anchor* score (ZNCC vs the never-changing seed), NOT the effective score. Measured on v3's post-set false lock: effective score = 1.000 (the adaptive template self-matches once locked on background), while anchor score collapses to 0.40–0.46 — vs 0.99 during the real lift. So any Suspect/Lost logic must threshold on identity (anchor) confidence, which the pipeline computes but discards. False confidence is also structural: headless auto-resume re-fabricates `Tracked` samples from a stale position after the object is gone, and a reseed-cluster cap whack-a-moles (the resume just finds another background patch that holds >60 frames). Clean fix: thread anchor/identity confidence through StepOutcome→Sample→PathPoint→export (~15 sites); mark low-identity-confidence Founds `Source::Suspect`; grade treats Suspect-while-absent as honest (not false confidence); terminal Lost after sustained Suspect; stop headless auto-resume once Lost. Side result: v4 (fable labels) at anchor-floor 0.5 already grades 100% within 0.1  **Done via confidence-threading.** The honest identity confidence (TemplateTracker anchor score; ColorTracker fill-fraction) now flows StepOutcome→Sample→PathPoint→CSV/JSON `confidence` column. `grade` (default trusted 0.7) splits a bar-absent tracked sample into `false_confidence` (confident lie) vs `suspect_while_absent` (honest doubt). Measured on v3: the post-set false lock reports confidence 0.46 → false_confidence 1→0, suspect 1. Deferred to 17.4b: terminal Lost state + stop headless auto-resume once Lost (this commit makes the dishonesty *visible/flaggable*; stopping the run entirely is the next step).plate-dia, mean 3.5px.
| 17.4b | Terminal `Lost` state (distinct from the recoverable `NeedsReseed` reseed-pause) after sustained low-identity-confidence tracking; stop headless auto-resume once `Lost` | S | done (sonnet-5, 2026-07-22) | Deferred piece of 17.4 (see 17.4's own Observations). Audit F5's "there is no way to express 'tracked, but wrong'" was only half-closed by 17.4's confidence-threading: the run kept going and headless kept reseeding itself onto the same untrustworthy position. **Design:** new `SessionState::Lost` (not a new `Source` variant — `Sample::confidence`, already threaded in 17.4, is what distinguishes it from a healthy `Tracked`) reached from `TrackingSession::step`'s `Found` branch when identity confidence stays below `accuracy::DEFAULT_TRUSTED_CONFIDENCE` (17.4's existing 0.7 honest-doubt cutoff — reused rather than adding a second tunable for the same concept) for `sustained_suspect_limit` *consecutive* `Found` frames — a new, dedicated knob (`TrackingSessionConfig::sustained_suspect_limit`, default 10), not a retune of the milestone's six existing ones per its "no threshold-tuning" framing: those all react to misses, this reacts to sustained low-confidence *hits*, a failure mode none of them can see. A `Miss` resets the streak (it's a different, already-handled failure mode via `miss_count`/`NeedsReseed`); a `Found` at/above the threshold resets it too. `step()` no-ops once `Lost`, same as `NeedsReseed`; `reseed()` still works from `Lost` (a human — GUI or otherwise — can still manually recover), it just isn't auto-invoked. **Headless (`cli.rs`):** unchanged reseed-loop only pattern-matches `SessionState::NeedsReseed`, so `Lost` never triggers auto-resume; `tracking::run_tracking_loop` explicitly returns `LoopOutcome::Stopped` on `Lost` (same convergent path a user Stop already takes → `Done` with the partial `BarPath`, not a silently-decoded rest-of-video with `step` reduced to a no-op) instead of `NeedsReseed`'s block-and-wait-for-a-reseed. `run_track` prints a distinct `eprintln!`/`tracing::warn!` line when the run ended via `Lost`, naming the frame and reseed-event count before it, instead of reporting a normal completion. **GUI scope, deliberately minimal:** `TrackingRunState`/side panel/toolbar/bottom bar are untouched — none of them exhaustively `match` on `SessionState` (all comparisons are `==`), so nothing broke, but `is_searching()`/`status_line()` don't have a `Lost`-specific case; a `Lost` run just falls through to Done shortly after (the worker doesn't block on it, unlike `NeedsReseed`), so it's a brief flash of ordinary-looking status text rather than a stuck bad state — acceptable for this pass, a follow-up if the GUI needs to explain *why* a run ended early. CONTEXT.md gained a "Lost" term. New tests: `session.rs` (`sustained_low_confidence_found_trips_lost`, `a_single_transient_low_confidence_frame_does_not_trip_lost`, `a_miss_between_low_confidence_founds_resets_the_suspect_streak`, `reseed_recovers_a_session_from_lost`) and `tracking.rs` (`run_tracking_loop_stops_immediately_when_session_goes_lost`). tracker-core: 217→221 tests; tracker-app: unit total 265 (was 264). Full workspace `cargo test --workspace`/`clippy --workspace -- -D warnings`/`fmt --check` all clean. |
| 17.5 | Plate-circle tracker: fit the plate rim per frame (circle fit seeded by the user's click), report the fitted centre as the Bar Path position; radius gives a per-frame pixel→metre scale (0.450 m competition plate) and a real lost signal; added to the `compare` matrix as a 7th strategy | M | done (sonnet-5, 2026-07-22) | Audit F6 (was 16.7; user-chosen 2026-07-21). Appearance-based identity forces a tradeoff we hit from both sides in one day: seed off-axis = 45 mm plate-spin artifact (x stdev 69.1px), seed on-axis = rotation-invariant but the chrome hub is smooth/specular so rack hardware out-scores it (x stdev 30.1px but drifts off entirely by 22s). No seed position is both rotation-invariant and discriminative — the object's real invariant is that it's a circle of known diameter. Must be evaluated under 17.1's accuracy metric, not assumed better. **Impl:** new `tracker-core/src/circle_tracker.rs`: `CircleTracker`/`CircleTrackerConfig` (builder, mirrors `TemplateTracker`/`ColorTracker`), pure/dependency-free, implements the shared `Tracker` trait so it drops into `TrackingSession`/`AnyTracker` unchanged. From-scratch gradient-direction Hough circle transform (no image crate): every pixel in the search window whose Sobel gradient magnitude clears `edge_threshold` casts two votes (one each sign — rim polarity, light-on-dark or dark-on-light, isn't known a priori) into a `(cx,cy,r)` accumulator across `min_radius..=max_radius`; the peak bin is the fitted circle. A straight edge (rack upright, barbell shaft, a rotated texture line on the plate face) never accumulates at one `(cx,cy)` the way a genuine rim's curvature does, which is the rotation-invariance property a TDD test exercises directly (a marker line rotated 15°→160° inside the disc moves the fitted centre <1.5px). Radius is folded into the "lost" signal without extending the shared `StepOutcome` enum: a candidate whose radius jumped >`max_radius_jump_fraction` (default 0.6) from the last *accepted* fit is rejected as `Miss` even with strong edge support — "sudden radius change is the real lost signal" per the task spec. `identity_confidence`/`score` are both the accumulator peak's edge-vote fraction (`votes / (2×edge_count)`); same velocity-reachability gate as the other two trackers (`motion::gate_radius`). **App wiring:** `TrackerSelection::Circle` (tracking.rs) + `AnyTracker::Circle`; never reachable via `Auto` (`suggest_tracker` has no circular-edge heuristic — deferred, noted in the variant's doc comment) so it's selected explicitly (`--tracker circle`, or the GUI combo — added, trivial 4th arm). `run_tracking_worker`'s tail (session/loop/reap/export) was extracted into `finish_tracking_run` so the Circle path (built directly, bypassing `TrackerKind` resolution) and the Template/Color path (via `suggest_tracker`) converge on identical session/export handling. `compare.rs`'s `strategy_matrix()` grew a 7th entry, `none/circle` (edge detection has its own smoothing; not varied across the filter axis), slug `none-circle`, score unit `edge-support`. **Grading (mandate — evaluated, not assumed better):** seeded on-axis at the same points used for the template runs (v3 frame 300 @ 284,94; v4 frame 300 @ 232,148) and graded via `compare`'s raw per-frame outcomes (bypassing `TrackingSession`'s `Lost` state, see caveat below) against `groundtruth/{v3,v4}.csv`, default 134px plate diameter, 0.1-dia tolerance: **v3: 17% within tolerance, mean 22.2px / 0.166 plate-dia, max 36.9px, 2781/2884 frames found; v4: 0% within tolerance (2 of 10 labels matched at all — most of the video is `Miss`), mean 22.1px / 0.165 plate-dia on the 2 it does match, only 597/3477 frames found.** Verdict: **not better than the post-17.3 template** (v3 85% within tolerance, mean 25-35px) — the circle tracker is *not* recommended as a replacement. It is directionally sane (v3's 22px mean error is in the same ballpark as the known ~29px plate-centre-to-sleeve-end offset per 16.6, i.e. it isn't randomly drifting, it's fitting a real but differently-defined point), but the from-scratch gradient-Hough edge vote is weak on this specular/low-contrast rig: real-footage edge-vote fractions came out ~1000x smaller than a clean-synthetic ring's (0.0008-0.05 vs the synthetic tests' ~0.02-0.15), and `min_edge_support`/`edge_threshold`/`min_radius..max_radius` needed hand-tuning off the defaults just to find *anything* at the seed. **Known caveat, not fixed in this task:** that same low edge-vote confidence, read through 17.4's shared `identity_confidence` vocabulary (calibrated for ZNCC/fill-fraction, which sit near 1.0 for a good match), trips 17.4b's sustained-low-confidence `Lost` after ~10 frames even while the geometric fit is still landing on the plate — so a real `track` run reports Lost almost immediately; the v3/v4 numbers above come from `compare`'s direct per-frame `Tracker::step` outcomes, which don't route through `TrackingSession`/`Lost`. Confidence for this tracker needs its own calibration (e.g. normalizing against a per-window expected-edge-density baseline) before `Lost`/`Suspect` are meaningful for it — left as follow-up, not silently patched around to make the number look better. Tests: 232 tracker-core (was 223, +9, all circle_tracker.rs), tracker-app unchanged net (matrix/slug tests updated for the 7th entry). `cargo test --workspace`/`clippy --workspace -- -D warnings`/`fmt --check` all clean. |
| 17.6 | Retire or gate the Color tracker: require a positive `suggest_tracker` result, or restrict to footage with a real Marker per CONTEXT.md | S | todo | On all 4 test videos every colour strategy carries the indistinct-colour note; on v3 it posts 0.27 mean fill-fraction with up to 39 reseeds. Its deceptively low jitter (a blob centroid wandering smoothly) actively misleads the benchmark table |

## Milestone 18 — GUI responsiveness (audit 2026-07-22)

Full audit + durable rules: [docs/gui-threading.md](docs/gui-threading.md).
Trigger: users repeatedly hit the OS "application is not responding" dialog,
most reliably during a long tracking run and while scrubbing. Root cause:
display-frame decoding is a synchronous `ffmpeg -ss` spawn + `child.wait()`
on the eframe main thread (`ensure_texture` → `SeekingFrameDecoder`), so the
winit event loop starves. Compute workers (tracking/thumbnail/export/benchmark)
are correctly off-thread — the *display decoder* is the one place the pattern
was not applied.

| ID | Task | Size | Status | Observations |
|----|------|------|--------|--------------|
| 18.1 | Async frame-decode worker: move `SeekingFrameDecoder`/`FrameCache` off the UI thread; `update` sends "want frame N" (coalesced to latest) and draws the most recent decoded texture, never calling `decode_frame` synchronously. Removes the freeze (findings G1/G2). | M | done (sonnet-5, 2026-07-22) | New `decode_worker.rs`, spawn+channel shape mirroring `tracking.rs`/`thumbnail_worker.rs`: `spawn_decode_worker` hands the `SeekingFrameDecoder` to a dedicated thread that wraps it in its own `FrameCache` (cap 16) — the worker, never the UI, is the sole decoder owner/caller now. UI sends `DecodeHandle::want(index)` (unbounded channel, non-blocking send); worker's `run_decode_worker` blocks on the *first* queued want, then drains every further already-queued want with `try_recv` and decodes only the last one — a burst of stale wants is dropped, not queued and worked through, so the worker can't fall further behind than one decode. Replies (`DecodeMessage::Decoded`/`Error`) drained with `try_recv` in a new `TrackerApp::poll_decode`, called from `update` alongside `poll_thumbnails`; texture upload stays on the UI thread as before. `TrackerApp` also keeps a small (cap 8) UI-side `frames` cache populated only from worker replies — used to draw the texture and to answer "do I already have this frame" without ever touching the worker/decoder synchronously. G2 (`video_panel.rs`'s seed-placement handler, `cache.get(seed.frame_index)`): now checks the UI-side `frames` cache first (the seed's usually on the already-displayed frame, so a hit); on a miss it records `pending_seed_suggestion` and calls `decode.want(...)`, and `poll_decode` applies `note_seed_suggestion` once the reply lands — no synchronous decode reachable from the paint path anymore. `Drop` teardown: `DecodeHandle` just drops its `want_tx`; the worker's next `recv()` returns `Err` and the thread exits — no long-lived child process to kill (each decode is its own short-lived `ffmpeg -ss`+`wait()`, already reaped inside `decode_frame` before the reply is sent), so there's no reap-deadlock risk to guard against here unlike `tracking.rs`'s streaming decoder. Tested: `decode_worker.rs`'s `run_decode_worker` is unit-tested directly against an in-memory fake `FrameDecoder` (no thread, no ffmpeg) for the coalescing burst-of-5-collapses-to-1 case, single round-trip, decode-failure→`Error`, and cache-hit-on-repeat; one real-thread end-to-end smoke test exercises `spawn_decode_worker`/`DecodeHandle` over actual channels+thread with a slow fake decoder. `seek_source.rs`'s real-ffmpeg test stays `#[ignore]`d as before — this task didn't touch that decoder, only where it's called from. tracker-app lib tests 265→270. Verified no synchronous `decode_frame`/`ffmpeg`/`child.wait()` remains reachable from `update`/`show`/paint: `cache.get`/`FrameCache`/`SeekingFrameDecoder` are gone from `app/mod.rs` and `video_panel.rs` entirely (only `decode_worker.rs`, `thumbnail_worker.rs` — already off-thread — and the headless `cli.rs`, which has no UI thread per `docs/gui-threading.md` rule 2, still reference them). Gates clean: `cargo test --workspace` (270+221+2 passed, 4 ignored), `cargo clippy --workspace -- -D warnings`, `cargo fmt --check`. Xvfb launch smoke (`xvfb-run` + real test video): starts, probes, opens, thumbnails decode, no panic/error in the log within an 8s window (can't reproduce the freeze or screenshot interactivity headlessly, so correctness here rests on the architecture argument above plus the coalescing unit tests, not the smoke run). |
| 18.2 | Off-thread `open_video`: `rfd::AsyncFileDialog` (or a picker thread) + move the `ffprobe` probe onto a worker so opening a video never stalls the render loop (findings G3/G4); "probing…" in the status bar | S | todo | |
| 18.3 | Responsiveness smoke + guardrail: add a scrub-storm + full-track + Finish-mid-run manual smoke step (Linux and macOS) to scripts/smoke-report.sh / RUNNING.md; link docs/gui-threading.md's checklist from the README Contributing section | S | todo | The checklist exists to stop humans *and* LLMs reintroducing a synchronous decode in `update` |
